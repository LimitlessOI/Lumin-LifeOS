#!/usr/bin/env node
/**
 * SYNOPSIS: Acceptance test for unknown product (auto-generated by executor).
 * @ssot docs/products/unknown/PRODUCT_HOME.md
 */
import fs from 'node:fs';
import path from 'node:path';
import { execSync } from 'node:child_process';
import { fileURLToPath } from 'node:url';

const ROOT = path.resolve(path.dirname(fileURLToPath(import.meta.url)), '..');
const fails = [];
let passes = 0;
function pass(msg) { passes++; console.log('PASS ' + msg); }
function fail(msg) { fails.push(msg); console.log('FAIL ' + msg); }

  try {
    if (!fs.existsSync(path.join(ROOT, 'db/migrations/20260704_create_students.sql'))) { fail('Missing: db/migrations/20260704_create_students.sql'); }
    else { const c = fs.readFileSync(path.join(ROOT, 'db/migrations/20260704_create_students.sql'), 'utf8'); /CREATE\s+TABLE/i.test(c) ? pass('db/migrations/20260704_create_students.sql exists + has CREATE TABLE') : fail('db/migrations/20260704_create_students.sql exists but no CREATE TABLE'); }
  } catch (e) { fail('db/migrations/20260704_create_students.sql: ' + e.message.split('\n')[0]); }
  try {
    if (!fs.existsSync(path.join(ROOT, 'db/migrations/20260704_create_courses.sql'))) { fail('Missing: db/migrations/20260704_create_courses.sql'); }
    else { const c = fs.readFileSync(path.join(ROOT, 'db/migrations/20260704_create_courses.sql'), 'utf8'); /CREATE\s+TABLE/i.test(c) ? pass('db/migrations/20260704_create_courses.sql exists + has CREATE TABLE') : fail('db/migrations/20260704_create_courses.sql exists but no CREATE TABLE'); }
  } catch (e) { fail('db/migrations/20260704_create_courses.sql: ' + e.message.split('\n')[0]); }
  try {
    if (!fs.existsSync(path.join(ROOT, 'db/migrations/20260704_create_enrollments.sql'))) { fail('Missing: db/migrations/20260704_create_enrollments.sql'); }
    else { const c = fs.readFileSync(path.join(ROOT, 'db/migrations/20260704_create_enrollments.sql'), 'utf8'); /CREATE\s+TABLE/i.test(c) ? pass('db/migrations/20260704_create_enrollments.sql exists + has CREATE TABLE') : fail('db/migrations/20260704_create_enrollments.sql exists but no CREATE TABLE'); }
  } catch (e) { fail('db/migrations/20260704_create_enrollments.sql: ' + e.message.split('\n')[0]); }
  try {
    if (!fs.existsSync(path.join(ROOT, 'services/course-service.js'))) { fail('Missing: services/course-service.js'); }
    else { execSync(`node -c "${path.join(ROOT, 'services/course-service.js')}"`, { encoding: 'utf8', stdio: 'pipe' }); pass('services/course-service.js exists + syntax OK'); }
  } catch (e) { fail('services/course-service.js: ' + e.message.split('\n')[0]); }
  try {
    if (!fs.existsSync(path.join(ROOT, 'routes/course-routes.js'))) { fail('Missing: routes/course-routes.js'); }
    else { execSync(`node -c "${path.join(ROOT, 'routes/course-routes.js')}"`, { encoding: 'utf8', stdio: 'pipe' }); pass('routes/course-routes.js exists + syntax OK'); }
  } catch (e) { fail('routes/course-routes.js: ' + e.message.split('\n')[0]); }

console.log('\nResults: ' + passes + ' passed, ' + fails.length + ' failed of 5 files');
if (fails.length) {
  console.error('FAILURES:', fails.join('; '));
  process.exit(1);
}
console.log('ALL CHECKS PASSED');
process.exit(0);
