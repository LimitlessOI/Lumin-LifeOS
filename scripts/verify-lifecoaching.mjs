#!/usr/bin/env node
/**
 * SYNOPSIS: Acceptance test for unknown product (auto-generated by executor).
 * @ssot docs/products/unknown/PRODUCT_HOME.md
 */
import fs from 'node:fs';
import path from 'node:path';
import { execSync } from 'node:child_process';
import { fileURLToPath } from 'node:url';

const ROOT = path.resolve(path.dirname(fileURLToPath(import.meta.url)), '..');
const fails = [];
let passes = 0;
function pass(msg) { passes++; console.log('PASS ' + msg); }
function fail(msg) { fails.push(msg); console.log('FAIL ' + msg); }

async function existsOnGitHub(filePath) {
  const token = process.env.GITHUB_TOKEN;
  if (!token) return false;
  try {
    const res = await fetch('https://api.github.com/repos/LimitlessOI/Lumin-LifeOS/contents/' + filePath, {
      headers: { Authorization: 'Bearer ' + token, Accept: 'application/vnd.github.v3+json' },
      signal: AbortSignal.timeout(5000),
    });
    if (!res.ok) return false;
    const data = await res.json();
    return data.size > 10;
  } catch { return false; }
}

async function checkFile(filePath, validator) {
  if (fs.existsSync(path.join(ROOT, filePath))) {
    return validator(path.join(ROOT, filePath));
  }
  if (await existsOnGitHub(filePath)) {
    pass(filePath + ' exists on GitHub');
    return;
  }
  fail('Missing: ' + filePath);
}

async function run() {
  await checkFile('db/migrations/20260704_create_conversation_messages.sql', (abs) => {
    const c = fs.readFileSync(abs, 'utf8');
    /CREATE\s+TABLE/i.test(c) ? pass('db/migrations/20260704_create_conversation_messages.sql exists + has CREATE TABLE') : fail('db/migrations/20260704_create_conversation_messages.sql exists but no CREATE TABLE');
  });
  await checkFile('db/migrations/20260704_create_adam_decisions.sql', (abs) => {
    const c = fs.readFileSync(abs, 'utf8');
    /CREATE\s+TABLE/i.test(c) ? pass('db/migrations/20260704_create_adam_decisions.sql exists + has CREATE TABLE') : fail('db/migrations/20260704_create_adam_decisions.sql exists but no CREATE TABLE');
  });
  await checkFile('db/migrations/20260704_create_adam_profile.sql', (abs) => {
    const c = fs.readFileSync(abs, 'utf8');
    /CREATE\s+TABLE/i.test(c) ? pass('db/migrations/20260704_create_adam_profile.sql exists + has CREATE TABLE') : fail('db/migrations/20260704_create_adam_profile.sql exists but no CREATE TABLE');
  });
  await checkFile('db/migrations/20260704_create_twin_ingest_control.sql', (abs) => {
    const c = fs.readFileSync(abs, 'utf8');
    /CREATE\s+TABLE/i.test(c) ? pass('db/migrations/20260704_create_twin_ingest_control.sql exists + has CREATE TABLE') : fail('db/migrations/20260704_create_twin_ingest_control.sql exists but no CREATE TABLE');
  });
  await checkFile('services/twin-auto-ingest.js', (abs) => {
    try { execSync(`node -c "${abs}"`, { encoding: 'utf8', stdio: 'pipe' }); pass('services/twin-auto-ingest.js exists + syntax OK'); }
    catch (e) { fail('services/twin-auto-ingest.js: ' + e.message.split('\n')[0]); }
  });
  await checkFile('services/adam-logger.js', (abs) => {
    try { execSync(`node -c "${abs}"`, { encoding: 'utf8', stdio: 'pipe' }); pass('services/adam-logger.js exists + syntax OK'); }
    catch (e) { fail('services/adam-logger.js: ' + e.message.split('\n')[0]); }
  });
  await checkFile('services/autonomy-scheduler.js', (abs) => {
    try { execSync(`node -c "${abs}"`, { encoding: 'utf8', stdio: 'pipe' }); pass('services/autonomy-scheduler.js exists + syntax OK'); }
    catch (e) { fail('services/autonomy-scheduler.js: ' + e.message.split('\n')[0]); }
  });
  await checkFile('routes/goals-routes.js', (abs) => {
    try { execSync(`node -c "${abs}"`, { encoding: 'utf8', stdio: 'pipe' }); pass('routes/goals-routes.js exists + syntax OK'); }
    catch (e) { fail('routes/goals-routes.js: ' + e.message.split('\n')[0]); }
  });
  await checkFile('routes/activities-routes.js', (abs) => {
    try { execSync(`node -c "${abs}"`, { encoding: 'utf8', stdio: 'pipe' }); pass('routes/activities-routes.js exists + syntax OK'); }
    catch (e) { fail('routes/activities-routes.js: ' + e.message.split('\n')[0]); }
  });
  await checkFile('routes/calendar-routes.js', (abs) => {
    try { execSync(`node -c "${abs}"`, { encoding: 'utf8', stdio: 'pipe' }); pass('routes/calendar-routes.js exists + syntax OK'); }
    catch (e) { fail('routes/calendar-routes.js: ' + e.message.split('\n')[0]); }
  });
  await checkFile('routes/coach-chat-routes.js', (abs) => {
    try { execSync(`node -c "${abs}"`, { encoding: 'utf8', stdio: 'pipe' }); pass('routes/coach-chat-routes.js exists + syntax OK'); }
    catch (e) { fail('routes/coach-chat-routes.js: ' + e.message.split('\n')[0]); }
  });
  await checkFile('routes/call-simulation-routes.js', (abs) => {
    try { execSync(`node -c "${abs}"`, { encoding: 'utf8', stdio: 'pipe' }); pass('routes/call-simulation-routes.js exists + syntax OK'); }
    catch (e) { fail('routes/call-simulation-routes.js: ' + e.message.split('\n')[0]); }
  });
  await checkFile('routes/perfect-day-routes.js', (abs) => {
    try { execSync(`node -c "${abs}"`, { encoding: 'utf8', stdio: 'pipe' }); pass('routes/perfect-day-routes.js exists + syntax OK'); }
    catch (e) { fail('routes/perfect-day-routes.js: ' + e.message.split('\n')[0]); }
  });
  await checkFile('routes/progress-routes.js', (abs) => {
    try { execSync(`node -c "${abs}"`, { encoding: 'utf8', stdio: 'pipe' }); pass('routes/progress-routes.js exists + syntax OK'); }
    catch (e) { fail('routes/progress-routes.js: ' + e.message.split('\n')[0]); }
  });

console.log('\nResults: ' + passes + ' passed, ' + fails.length + ' failed of 14 files');
if (fails.length) {
  console.error('FAILURES:', fails.join('; '));
  process.exit(1);
}
console.log('ALL CHECKS PASSED');
process.exit(0);
}
run();
