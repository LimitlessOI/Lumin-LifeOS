#!/usr/bin/env node
/**
 * SYNOPSIS: Acceptance test for unknown product (auto-generated by executor).
 * @ssot docs/products/unknown/PRODUCT_HOME.md
 */
import fs from 'node:fs';
import path from 'node:path';
import { execSync } from 'node:child_process';
import { fileURLToPath } from 'node:url';

const ROOT = path.resolve(path.dirname(fileURLToPath(import.meta.url)), '..');
const fails = [];
let passes = 0;
function pass(msg) { passes++; console.log('PASS ' + msg); }
function fail(msg) { fails.push(msg); console.log('FAIL ' + msg); }

async function existsOnGitHub(filePath) {
  const token = process.env.GITHUB_TOKEN;
  if (!token) return false;
  try {
    const res = await fetch('https://api.github.com/repos/LimitlessOI/Lumin-LifeOS/contents/' + filePath, {
      headers: { Authorization: 'Bearer ' + token, Accept: 'application/vnd.github.v3+json' },
      signal: AbortSignal.timeout(5000),
    });
    if (!res.ok) return false;
    const data = await res.json();
    return data.size > 10;
  } catch { return false; }
}

async function checkFile(filePath, validator) {
  if (fs.existsSync(path.join(ROOT, filePath))) {
    return validator(path.join(ROOT, filePath));
  }
  if (await existsOnGitHub(filePath)) {
    pass(filePath + ' exists on GitHub');
    return;
  }
  fail('Missing: ' + filePath);
}

async function run() {
  await checkFile('db/migrations/20260704_create_token_usage_log.sql', (abs) => {
    const c = fs.readFileSync(abs, 'utf8');
    /CREATE\s+TABLE/i.test(c) ? pass('db/migrations/20260704_create_token_usage_log.sql exists + has CREATE TABLE') : fail('db/migrations/20260704_create_token_usage_log.sql exists but no CREATE TABLE');
  });
  await checkFile('db/migrations/20260704_create_free_tier_usage.sql', (abs) => {
    const c = fs.readFileSync(abs, 'utf8');
    /CREATE\s+TABLE/i.test(c) ? pass('db/migrations/20260704_create_free_tier_usage.sql exists + has CREATE TABLE') : fail('db/migrations/20260704_create_free_tier_usage.sql exists but no CREATE TABLE');
  });
  await checkFile('services/council-service.js', (abs) => {
    try { execSync(`node -c "${abs}"`, { encoding: 'utf8', stdio: 'pipe' }); pass('services/council-service.js exists + syntax OK'); }
    catch (e) { fail('services/council-service.js: ' + e.message.split('\n')[0]); }
  });
  await checkFile('services/token-optimizer.js', (abs) => {
    try { execSync(`node -c "${abs}"`, { encoding: 'utf8', stdio: 'pipe' }); pass('services/token-optimizer.js exists + syntax OK'); }
    catch (e) { fail('services/token-optimizer.js: ' + e.message.split('\n')[0]); }
  });
  await checkFile('services/savings-ledger.js', (abs) => {
    try { execSync(`node -c "${abs}"`, { encoding: 'utf8', stdio: 'pipe' }); pass('services/savings-ledger.js exists + syntax OK'); }
    catch (e) { fail('services/savings-ledger.js: ' + e.message.split('\n')[0]); }
  });
  await checkFile('services/free-tier-governor.js', (abs) => {
    try { execSync(`node -c "${abs}"`, { encoding: 'utf8', stdio: 'pipe' }); pass('services/free-tier-governor.js exists + syntax OK'); }
    catch (e) { fail('services/free-tier-governor.js: ' + e.message.split('\n')[0]); }
  });
  await checkFile('services/ai-model-selector.js', (abs) => {
    try { execSync(`node -c "${abs}"`, { encoding: 'utf8', stdio: 'pipe' }); pass('services/ai-model-selector.js exists + syntax OK'); }
    catch (e) { fail('services/ai-model-selector.js: ' + e.message.split('\n')[0]); }
  });
  await checkFile('services/consensus-service.js', (abs) => {
    try { execSync(`node -c "${abs}"`, { encoding: 'utf8', stdio: 'pipe' }); pass('services/consensus-service.js exists + syntax OK'); }
    catch (e) { fail('services/consensus-service.js: ' + e.message.split('\n')[0]); }
  });
  await checkFile('services/response-cache.js', (abs) => {
    try { execSync(`node -c "${abs}"`, { encoding: 'utf8', stdio: 'pipe' }); pass('services/response-cache.js exists + syntax OK'); }
    catch (e) { fail('services/response-cache.js: ' + e.message.split('\n')[0]); }
  });
  await checkFile('services/ai-guard.js', (abs) => {
    try { execSync(`node -c "${abs}"`, { encoding: 'utf8', stdio: 'pipe' }); pass('services/ai-guard.js exists + syntax OK'); }
    catch (e) { fail('services/ai-guard.js: ' + e.message.split('\n')[0]); }
  });
  await checkFile('services/prompt-translator.js', (abs) => {
    try { execSync(`node -c "${abs}"`, { encoding: 'utf8', stdio: 'pipe' }); pass('services/prompt-translator.js exists + syntax OK'); }
    catch (e) { fail('services/prompt-translator.js: ' + e.message.split('\n')[0]); }
  });
  await checkFile('services/lifeos-gate-change-proposals.js', (abs) => {
    try { execSync(`node -c "${abs}"`, { encoding: 'utf8', stdio: 'pipe' }); pass('services/lifeos-gate-change-proposals.js exists + syntax OK'); }
    catch (e) { fail('services/lifeos-gate-change-proposals.js: ' + e.message.split('\n')[0]); }
  });
  await checkFile('routes/lifeos-gate-change-routes.js', (abs) => {
    try { execSync(`node -c "${abs}"`, { encoding: 'utf8', stdio: 'pipe' }); pass('routes/lifeos-gate-change-routes.js exists + syntax OK'); }
    catch (e) { fail('routes/lifeos-gate-change-routes.js: ' + e.message.split('\n')[0]); }
  });
  await checkFile('public/overlay/council-dashboard.html', (abs) => {
    const c = fs.readFileSync(abs, 'utf8');
    c.length > 50 ? pass('public/overlay/council-dashboard.html exists (' + c.length + ' bytes)') : fail('public/overlay/council-dashboard.html too small');
  });

console.log('\nResults: ' + passes + ' passed, ' + fails.length + ' failed of 14 files');
if (fails.length) {
  console.error('FAILURES:', fails.join('; '));
  process.exit(1);
}
console.log('ALL CHECKS PASSED');
process.exit(0);
}
run();
