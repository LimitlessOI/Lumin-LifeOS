#!/usr/bin/env node
/**
 * SYNOPSIS: Acceptance test for unknown product (auto-generated by executor).
 * @ssot docs/products/unknown/PRODUCT_HOME.md
 */
import fs from 'node:fs';
import path from 'node:path';
import { execSync } from 'node:child_process';
import { fileURLToPath } from 'node:url';

const ROOT = path.resolve(path.dirname(fileURLToPath(import.meta.url)), '..');
const fails = [];
let passes = 0;
function pass(msg) { passes++; console.log('PASS ' + msg); }
function fail(msg) { fails.push(msg); console.log('FAIL ' + msg); }

  try {
    if (!fs.existsSync(path.join(ROOT, 'db/migrations/20260704_create_coaching_sessions_table.sql'))) { fail('Missing: db/migrations/20260704_create_coaching_sessions_table.sql'); }
    else { const c = fs.readFileSync(path.join(ROOT, 'db/migrations/20260704_create_coaching_sessions_table.sql'), 'utf8'); /CREATE\s+TABLE/i.test(c) ? pass('db/migrations/20260704_create_coaching_sessions_table.sql exists + has CREATE TABLE') : fail('db/migrations/20260704_create_coaching_sessions_table.sql exists but no CREATE TABLE'); }
  } catch (e) { fail('db/migrations/20260704_create_coaching_sessions_table.sql: ' + e.message.split('\n')[0]); }
  try {
    if (!fs.existsSync(path.join(ROOT, 'services/ai-coach-service.js'))) { fail('Missing: services/ai-coach-service.js'); }
    else { execSync(`node -c "${path.join(ROOT, 'services/ai-coach-service.js')}"`, { encoding: 'utf8', stdio: 'pipe' }); pass('services/ai-coach-service.js exists + syntax OK'); }
  } catch (e) { fail('services/ai-coach-service.js: ' + e.message.split('\n')[0]); }
  try {
    if (!fs.existsSync(path.join(ROOT, 'services/content-engine-service.js'))) { fail('Missing: services/content-engine-service.js'); }
    else { execSync(`node -c "${path.join(ROOT, 'services/content-engine-service.js')}"`, { encoding: 'utf8', stdio: 'pipe' }); pass('services/content-engine-service.js exists + syntax OK'); }
  } catch (e) { fail('services/content-engine-service.js: ' + e.message.split('\n')[0]); }
  try {
    if (!fs.existsSync(path.join(ROOT, 'routes/socialmediaos-routes.js'))) { fail('Missing: routes/socialmediaos-routes.js'); }
    else { execSync(`node -c "${path.join(ROOT, 'routes/socialmediaos-routes.js')}"`, { encoding: 'utf8', stdio: 'pipe' }); pass('routes/socialmediaos-routes.js exists + syntax OK'); }
  } catch (e) { fail('routes/socialmediaos-routes.js: ' + e.message.split('\n')[0]); }
  try {
    if (!fs.existsSync(path.join(ROOT, 'public/overlay/consent.html'))) { fail('Missing: public/overlay/consent.html'); }
    else { const c = fs.readFileSync(path.join(ROOT, 'public/overlay/consent.html'), 'utf8'); c.length > 50 ? pass('public/overlay/consent.html exists (' + c.length + ' bytes)') : fail('public/overlay/consent.html too small'); }
  } catch (e) { fail('public/overlay/consent.html: ' + e.message.split('\n')[0]); }

console.log('\nResults: ' + passes + ' passed, ' + fails.length + ' failed of 5 files');
if (fails.length) {
  console.error('FAILURES:', fails.join('; '));
  process.exit(1);
}
console.log('ALL CHECKS PASSED');
process.exit(0);
