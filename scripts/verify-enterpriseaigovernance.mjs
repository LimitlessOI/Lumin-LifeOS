#!/usr/bin/env node
/**
 * SYNOPSIS: Acceptance test for unknown product (auto-generated by executor).
 * @ssot docs/products/unknown/PRODUCT_HOME.md
 */
import fs from 'node:fs';
import path from 'node:path';
import { execSync } from 'node:child_process';
import { fileURLToPath } from 'node:url';

const ROOT = path.resolve(path.dirname(fileURLToPath(import.meta.url)), '..');
const fails = [];
let passes = 0;
function pass(msg) { passes++; console.log('PASS ' + msg); }
function fail(msg) { fails.push(msg); console.log('FAIL ' + msg); }

  try {
    if (!fs.existsSync(path.join(ROOT, 'db/migrations/20260705_create_audit_logs.sql'))) { fail('Missing: db/migrations/20260705_create_audit_logs.sql'); }
    else { const c = fs.readFileSync(path.join(ROOT, 'db/migrations/20260705_create_audit_logs.sql'), 'utf8'); /CREATE\s+TABLE/i.test(c) ? pass('db/migrations/20260705_create_audit_logs.sql exists + has CREATE TABLE') : fail('db/migrations/20260705_create_audit_logs.sql exists but no CREATE TABLE'); }
  } catch (e) { fail('db/migrations/20260705_create_audit_logs.sql: ' + e.message.split('\n')[0]); }
  try {
    if (!fs.existsSync(path.join(ROOT, 'services/llm-egress-proxy.js'))) { fail('Missing: services/llm-egress-proxy.js'); }
    else { execSync(`node -c "${path.join(ROOT, 'services/llm-egress-proxy.js')}"`, { encoding: 'utf8', stdio: 'pipe' }); pass('services/llm-egress-proxy.js exists + syntax OK'); }
  } catch (e) { fail('services/llm-egress-proxy.js: ' + e.message.split('\n')[0]); }
  try {
    if (!fs.existsSync(path.join(ROOT, 'routes/egress-proxy-routes.js'))) { fail('Missing: routes/egress-proxy-routes.js'); }
    else { execSync(`node -c "${path.join(ROOT, 'routes/egress-proxy-routes.js')}"`, { encoding: 'utf8', stdio: 'pipe' }); pass('routes/egress-proxy-routes.js exists + syntax OK'); }
  } catch (e) { fail('routes/egress-proxy-routes.js: ' + e.message.split('\n')[0]); }
  try {
    if (!fs.existsSync(path.join(ROOT, 'public/overlay/compliance-dashboard.html'))) { fail('Missing: public/overlay/compliance-dashboard.html'); }
    else { const c = fs.readFileSync(path.join(ROOT, 'public/overlay/compliance-dashboard.html'), 'utf8'); c.length > 50 ? pass('public/overlay/compliance-dashboard.html exists (' + c.length + ' bytes)') : fail('public/overlay/compliance-dashboard.html too small'); }
  } catch (e) { fail('public/overlay/compliance-dashboard.html: ' + e.message.split('\n')[0]); }

console.log('\nResults: ' + passes + ' passed, ' + fails.length + ' failed of 4 files');
if (fails.length) {
  console.error('FAILURES:', fails.join('; '));
  process.exit(1);
}
console.log('ALL CHECKS PASSED');
process.exit(0);
