#!/usr/bin/env node
/**
 * SYNOPSIS: Acceptance test for unknown product (auto-generated by executor).
 * @ssot docs/products/unknown/PRODUCT_HOME.md
 */
import fs from 'node:fs';
import path from 'node:path';
import { execSync } from 'node:child_process';
import { fileURLToPath } from 'node:url';

const ROOT = path.resolve(path.dirname(fileURLToPath(import.meta.url)), '..');
const fails = [];
let passes = 0;
function pass(msg) { passes++; console.log('PASS ' + msg); }
function fail(msg) { fails.push(msg); console.log('FAIL ' + msg); }

  try {
    if (!fs.existsSync(path.join(ROOT, 'db/migrations/20260704_create_children_table.sql'))) { fail('Missing: db/migrations/20260704_create_children_table.sql'); }
    else { const c = fs.readFileSync(path.join(ROOT, 'db/migrations/20260704_create_children_table.sql'), 'utf8'); /CREATE\s+TABLE/i.test(c) ? pass('db/migrations/20260704_create_children_table.sql exists + has CREATE TABLE') : fail('db/migrations/20260704_create_children_table.sql exists but no CREATE TABLE'); }
  } catch (e) { fail('db/migrations/20260704_create_children_table.sql: ' + e.message.split('\n')[0]); }
  try {
    if (!fs.existsSync(path.join(ROOT, 'db/migrations/20260704_create_achievements_table.sql'))) { fail('Missing: db/migrations/20260704_create_achievements_table.sql'); }
    else { const c = fs.readFileSync(path.join(ROOT, 'db/migrations/20260704_create_achievements_table.sql'), 'utf8'); /CREATE\s+TABLE/i.test(c) ? pass('db/migrations/20260704_create_achievements_table.sql exists + has CREATE TABLE') : fail('db/migrations/20260704_create_achievements_table.sql exists but no CREATE TABLE'); }
  } catch (e) { fail('db/migrations/20260704_create_achievements_table.sql: ' + e.message.split('\n')[0]); }
  try {
    if (!fs.existsSync(path.join(ROOT, 'services/future-self-simulator.js'))) { fail('Missing: services/future-self-simulator.js'); }
    else { execSync(`node -c "${path.join(ROOT, 'services/future-self-simulator.js')}"`, { encoding: 'utf8', stdio: 'pipe' }); pass('services/future-self-simulator.js exists + syntax OK'); }
  } catch (e) { fail('services/future-self-simulator.js: ' + e.message.split('\n')[0]); }
  try {
    if (!fs.existsSync(path.join(ROOT, 'services/confidence-architecture.js'))) { fail('Missing: services/confidence-architecture.js'); }
    else { execSync(`node -c "${path.join(ROOT, 'services/confidence-architecture.js')}"`, { encoding: 'utf8', stdio: 'pipe' }); pass('services/confidence-architecture.js exists + syntax OK'); }
  } catch (e) { fail('services/confidence-architecture.js: ' + e.message.split('\n')[0]); }
  try {
    if (!fs.existsSync(path.join(ROOT, 'routes/future-self-simulator-routes.js'))) { fail('Missing: routes/future-self-simulator-routes.js'); }
    else { execSync(`node -c "${path.join(ROOT, 'routes/future-self-simulator-routes.js')}"`, { encoding: 'utf8', stdio: 'pipe' }); pass('routes/future-self-simulator-routes.js exists + syntax OK'); }
  } catch (e) { fail('routes/future-self-simulator-routes.js: ' + e.message.split('\n')[0]); }
  try {
    if (!fs.existsSync(path.join(ROOT, 'routes/confidence-architecture-routes.js'))) { fail('Missing: routes/confidence-architecture-routes.js'); }
    else { execSync(`node -c "${path.join(ROOT, 'routes/confidence-architecture-routes.js')}"`, { encoding: 'utf8', stdio: 'pipe' }); pass('routes/confidence-architecture-routes.js exists + syntax OK'); }
  } catch (e) { fail('routes/confidence-architecture-routes.js: ' + e.message.split('\n')[0]); }
  try {
    if (!fs.existsSync(path.join(ROOT, 'routes/belonging-guarantee-routes.js'))) { fail('Missing: routes/belonging-guarantee-routes.js'); }
    else { execSync(`node -c "${path.join(ROOT, 'routes/belonging-guarantee-routes.js')}"`, { encoding: 'utf8', stdio: 'pipe' }); pass('routes/belonging-guarantee-routes.js exists + syntax OK'); }
  } catch (e) { fail('routes/belonging-guarantee-routes.js: ' + e.message.split('\n')[0]); }
  try {
    if (!fs.existsSync(path.join(ROOT, 'public/overlay/future-self-simulator.html'))) { fail('Missing: public/overlay/future-self-simulator.html'); }
    else { const c = fs.readFileSync(path.join(ROOT, 'public/overlay/future-self-simulator.html'), 'utf8'); c.length > 50 ? pass('public/overlay/future-self-simulator.html exists (' + c.length + ' bytes)') : fail('public/overlay/future-self-simulator.html too small'); }
  } catch (e) { fail('public/overlay/future-self-simulator.html: ' + e.message.split('\n')[0]); }

console.log('\nResults: ' + passes + ' passed, ' + fails.length + ' failed of 8 files');
if (fails.length) {
  console.error('FAILURES:', fails.join('; '));
  process.exit(1);
}
console.log('ALL CHECKS PASSED');
process.exit(0);
