#!/usr/bin/env node
/**
 * SYNOPSIS: Acceptance test for unknown product (auto-generated by executor).
 * @ssot docs/products/unknown/PRODUCT_HOME.md
 */
import fs from 'node:fs';
import path from 'node:path';
import { execSync } from 'node:child_process';
import { fileURLToPath } from 'node:url';

const ROOT = path.resolve(path.dirname(fileURLToPath(import.meta.url)), '..');
const fails = [];
let passes = 0;
function pass(msg) { passes++; console.log('PASS ' + msg); }
function fail(msg) { fails.push(msg); console.log('FAIL ' + msg); }

  try {
    if (!fs.existsSync(path.join(ROOT, 'db/migrations/20260704_create_creator_channels.sql'))) { fail('Missing: db/migrations/20260704_create_creator_channels.sql'); }
    else { const c = fs.readFileSync(path.join(ROOT, 'db/migrations/20260704_create_creator_channels.sql'), 'utf8'); /CREATE\s+TABLE/i.test(c) ? pass('db/migrations/20260704_create_creator_channels.sql exists + has CREATE TABLE') : fail('db/migrations/20260704_create_creator_channels.sql exists but no CREATE TABLE'); }
  } catch (e) { fail('db/migrations/20260704_create_creator_channels.sql: ' + e.message.split('\n')[0]); }
  try {
    if (!fs.existsSync(path.join(ROOT, 'db/migrations/20260704_create_creator_likeness_profiles.sql'))) { fail('Missing: db/migrations/20260704_create_creator_likeness_profiles.sql'); }
    else { const c = fs.readFileSync(path.join(ROOT, 'db/migrations/20260704_create_creator_likeness_profiles.sql'), 'utf8'); /CREATE\s+TABLE/i.test(c) ? pass('db/migrations/20260704_create_creator_likeness_profiles.sql exists + has CREATE TABLE') : fail('db/migrations/20260704_create_creator_likeness_profiles.sql exists but no CREATE TABLE'); }
  } catch (e) { fail('db/migrations/20260704_create_creator_likeness_profiles.sql: ' + e.message.split('\n')[0]); }
  try {
    if (!fs.existsSync(path.join(ROOT, 'db/migrations/20260704_create_creator_scripts.sql'))) { fail('Missing: db/migrations/20260704_create_creator_scripts.sql'); }
    else { const c = fs.readFileSync(path.join(ROOT, 'db/migrations/20260704_create_creator_scripts.sql'), 'utf8'); /CREATE\s+TABLE/i.test(c) ? pass('db/migrations/20260704_create_creator_scripts.sql exists + has CREATE TABLE') : fail('db/migrations/20260704_create_creator_scripts.sql exists but no CREATE TABLE'); }
  } catch (e) { fail('db/migrations/20260704_create_creator_scripts.sql: ' + e.message.split('\n')[0]); }
  try {
    if (!fs.existsSync(path.join(ROOT, 'db/migrations/20260704_create_creator_videos.sql'))) { fail('Missing: db/migrations/20260704_create_creator_videos.sql'); }
    else { const c = fs.readFileSync(path.join(ROOT, 'db/migrations/20260704_create_creator_videos.sql'), 'utf8'); /CREATE\s+TABLE/i.test(c) ? pass('db/migrations/20260704_create_creator_videos.sql exists + has CREATE TABLE') : fail('db/migrations/20260704_create_creator_videos.sql exists but no CREATE TABLE'); }
  } catch (e) { fail('db/migrations/20260704_create_creator_videos.sql: ' + e.message.split('\n')[0]); }
  try {
    if (!fs.existsSync(path.join(ROOT, 'db/migrations/20260704_create_creator_assets.sql'))) { fail('Missing: db/migrations/20260704_create_creator_assets.sql'); }
    else { const c = fs.readFileSync(path.join(ROOT, 'db/migrations/20260704_create_creator_assets.sql'), 'utf8'); /CREATE\s+TABLE/i.test(c) ? pass('db/migrations/20260704_create_creator_assets.sql exists + has CREATE TABLE') : fail('db/migrations/20260704_create_creator_assets.sql exists but no CREATE TABLE'); }
  } catch (e) { fail('db/migrations/20260704_create_creator_assets.sql: ' + e.message.split('\n')[0]); }
  try {
    if (!fs.existsSync(path.join(ROOT, 'db/migrations/20260704_create_creator_performance_memory.sql'))) { fail('Missing: db/migrations/20260704_create_creator_performance_memory.sql'); }
    else { const c = fs.readFileSync(path.join(ROOT, 'db/migrations/20260704_create_creator_performance_memory.sql'), 'utf8'); /CREATE\s+TABLE/i.test(c) ? pass('db/migrations/20260704_create_creator_performance_memory.sql exists + has CREATE TABLE') : fail('db/migrations/20260704_create_creator_performance_memory.sql exists but no CREATE TABLE'); }
  } catch (e) { fail('db/migrations/20260704_create_creator_performance_memory.sql: ' + e.message.split('\n')[0]); }
  try {
    if (!fs.existsSync(path.join(ROOT, 'services/creator-media-service.js'))) { fail('Missing: services/creator-media-service.js'); }
    else { execSync(`node -c "${path.join(ROOT, 'services/creator-media-service.js')}"`, { encoding: 'utf8', stdio: 'pipe' }); pass('services/creator-media-service.js exists + syntax OK'); }
  } catch (e) { fail('services/creator-media-service.js: ' + e.message.split('\n')[0]); }
  try {
    if (!fs.existsSync(path.join(ROOT, 'services/channel-memory.js'))) { fail('Missing: services/channel-memory.js'); }
    else { execSync(`node -c "${path.join(ROOT, 'services/channel-memory.js')}"`, { encoding: 'utf8', stdio: 'pipe' }); pass('services/channel-memory.js exists + syntax OK'); }
  } catch (e) { fail('services/channel-memory.js: ' + e.message.split('\n')[0]); }

console.log('\nResults: ' + passes + ' passed, ' + fails.length + ' failed of 8 files');
if (fails.length) {
  console.error('FAILURES:', fails.join('; '));
  process.exit(1);
}
console.log('ALL CHECKS PASSED');
process.exit(0);
