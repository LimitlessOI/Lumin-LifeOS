#!/usr/bin/env node
/**
 * SYNOPSIS: Acceptance test for unknown product (auto-generated by executor).
 * @ssot docs/products/unknown/PRODUCT_HOME.md
 */
import fs from 'node:fs';
import path from 'node:path';
import { execSync } from 'node:child_process';
import { fileURLToPath } from 'node:url';

const ROOT = path.resolve(path.dirname(fileURLToPath(import.meta.url)), '..');
const fails = [];
let passes = 0;
function pass(msg) { passes++; console.log('PASS ' + msg); }
function fail(msg) { fails.push(msg); console.log('FAIL ' + msg); }

  try {
    if (!fs.existsSync(path.join(ROOT, 'db/migrations/20260704_create_crm_contacts.sql'))) { fail('Missing: db/migrations/20260704_create_crm_contacts.sql'); }
    else { const c = fs.readFileSync(path.join(ROOT, 'db/migrations/20260704_create_crm_contacts.sql'), 'utf8'); /CREATE\s+TABLE/i.test(c) ? pass('db/migrations/20260704_create_crm_contacts.sql exists + has CREATE TABLE') : fail('db/migrations/20260704_create_crm_contacts.sql exists but no CREATE TABLE'); }
  } catch (e) { fail('db/migrations/20260704_create_crm_contacts.sql: ' + e.message.split('\n')[0]); }
  try {
    if (!fs.existsSync(path.join(ROOT, 'db/migrations/20260704_create_crm_sequences.sql'))) { fail('Missing: db/migrations/20260704_create_crm_sequences.sql'); }
    else { const c = fs.readFileSync(path.join(ROOT, 'db/migrations/20260704_create_crm_sequences.sql'), 'utf8'); /CREATE\s+TABLE/i.test(c) ? pass('db/migrations/20260704_create_crm_sequences.sql exists + has CREATE TABLE') : fail('db/migrations/20260704_create_crm_sequences.sql exists but no CREATE TABLE'); }
  } catch (e) { fail('db/migrations/20260704_create_crm_sequences.sql: ' + e.message.split('\n')[0]); }
  try {
    if (!fs.existsSync(path.join(ROOT, 'db/migrations/20260704_create_crm_sequence_enrollments.sql'))) { fail('Missing: db/migrations/20260704_create_crm_sequence_enrollments.sql'); }
    else { const c = fs.readFileSync(path.join(ROOT, 'db/migrations/20260704_create_crm_sequence_enrollments.sql'), 'utf8'); /CREATE\s+TABLE/i.test(c) ? pass('db/migrations/20260704_create_crm_sequence_enrollments.sql exists + has CREATE TABLE') : fail('db/migrations/20260704_create_crm_sequence_enrollments.sql exists but no CREATE TABLE'); }
  } catch (e) { fail('db/migrations/20260704_create_crm_sequence_enrollments.sql: ' + e.message.split('\n')[0]); }
  try {
    if (!fs.existsSync(path.join(ROOT, 'db/migrations/20260704_create_crm_messages.sql'))) { fail('Missing: db/migrations/20260704_create_crm_messages.sql'); }
    else { const c = fs.readFileSync(path.join(ROOT, 'db/migrations/20260704_create_crm_messages.sql'), 'utf8'); /CREATE\s+TABLE/i.test(c) ? pass('db/migrations/20260704_create_crm_messages.sql exists + has CREATE TABLE') : fail('db/migrations/20260704_create_crm_messages.sql exists but no CREATE TABLE'); }
  } catch (e) { fail('db/migrations/20260704_create_crm_messages.sql: ' + e.message.split('\n')[0]); }
  try {
    if (!fs.existsSync(path.join(ROOT, 'db/migrations/20260704_create_crm_replies.sql'))) { fail('Missing: db/migrations/20260704_create_crm_replies.sql'); }
    else { const c = fs.readFileSync(path.join(ROOT, 'db/migrations/20260704_create_crm_replies.sql'), 'utf8'); /CREATE\s+TABLE/i.test(c) ? pass('db/migrations/20260704_create_crm_replies.sql exists + has CREATE TABLE') : fail('db/migrations/20260704_create_crm_replies.sql exists but no CREATE TABLE'); }
  } catch (e) { fail('db/migrations/20260704_create_crm_replies.sql: ' + e.message.split('\n')[0]); }
  try {
    if (!fs.existsSync(path.join(ROOT, 'db/migrations/20260704_create_email_events.sql'))) { fail('Missing: db/migrations/20260704_create_email_events.sql'); }
    else { const c = fs.readFileSync(path.join(ROOT, 'db/migrations/20260704_create_email_events.sql'), 'utf8'); /CREATE\s+TABLE/i.test(c) ? pass('db/migrations/20260704_create_email_events.sql exists + has CREATE TABLE') : fail('db/migrations/20260704_create_email_events.sql exists but no CREATE TABLE'); }
  } catch (e) { fail('db/migrations/20260704_create_email_events.sql: ' + e.message.split('\n')[0]); }
  try {
    if (!fs.existsSync(path.join(ROOT, 'db/migrations/20260704_create_email_suppressions.sql'))) { fail('Missing: db/migrations/20260704_create_email_suppressions.sql'); }
    else { const c = fs.readFileSync(path.join(ROOT, 'db/migrations/20260704_create_email_suppressions.sql'), 'utf8'); /CREATE\s+TABLE/i.test(c) ? pass('db/migrations/20260704_create_email_suppressions.sql exists + has CREATE TABLE') : fail('db/migrations/20260704_create_email_suppressions.sql exists but no CREATE TABLE'); }
  } catch (e) { fail('db/migrations/20260704_create_email_suppressions.sql: ' + e.message.split('\n')[0]); }
  try {
    if (!fs.existsSync(path.join(ROOT, 'db/migrations/20260704_create_outbound_consent.sql'))) { fail('Missing: db/migrations/20260704_create_outbound_consent.sql'); }
    else { const c = fs.readFileSync(path.join(ROOT, 'db/migrations/20260704_create_outbound_consent.sql'), 'utf8'); /CREATE\s+TABLE/i.test(c) ? pass('db/migrations/20260704_create_outbound_consent.sql exists + has CREATE TABLE') : fail('db/migrations/20260704_create_outbound_consent.sql exists but no CREATE TABLE'); }
  } catch (e) { fail('db/migrations/20260704_create_outbound_consent.sql: ' + e.message.split('\n')[0]); }
  try {
    if (!fs.existsSync(path.join(ROOT, 'db/migrations/20260704_create_prospect_sites.sql'))) { fail('Missing: db/migrations/20260704_create_prospect_sites.sql'); }
    else { const c = fs.readFileSync(path.join(ROOT, 'db/migrations/20260704_create_prospect_sites.sql'), 'utf8'); /CREATE\s+TABLE/i.test(c) ? pass('db/migrations/20260704_create_prospect_sites.sql exists + has CREATE TABLE') : fail('db/migrations/20260704_create_prospect_sites.sql exists but no CREATE TABLE'); }
  } catch (e) { fail('db/migrations/20260704_create_prospect_sites.sql: ' + e.message.split('\n')[0]); }
  try {
    if (!fs.existsSync(path.join(ROOT, 'routes/outreach-crm-routes.js'))) { fail('Missing: routes/outreach-crm-routes.js'); }
    else { execSync(`node -c "${path.join(ROOT, 'routes/outreach-crm-routes.js')}"`, { encoding: 'utf8', stdio: 'pipe' }); pass('routes/outreach-crm-routes.js exists + syntax OK'); }
  } catch (e) { fail('routes/outreach-crm-routes.js: ' + e.message.split('\n')[0]); }
  try {
    if (!fs.existsSync(path.join(ROOT, 'services/autonomy-scheduler.js'))) { fail('Missing: services/autonomy-scheduler.js'); }
    else { execSync(`node -c "${path.join(ROOT, 'services/autonomy-scheduler.js')}"`, { encoding: 'utf8', stdio: 'pipe' }); pass('services/autonomy-scheduler.js exists + syntax OK'); }
  } catch (e) { fail('services/autonomy-scheduler.js: ' + e.message.split('\n')[0]); }

console.log('\nResults: ' + passes + ' passed, ' + fails.length + ' failed of 11 files');
if (fails.length) {
  console.error('FAILURES:', fails.join('; '));
  process.exit(1);
}
console.log('ALL CHECKS PASSED');
process.exit(0);
