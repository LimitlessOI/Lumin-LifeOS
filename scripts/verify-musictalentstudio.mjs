#!/usr/bin/env node
/**
 * SYNOPSIS: Acceptance test for unknown product (auto-generated by executor).
 * @ssot docs/products/unknown/PRODUCT_HOME.md
 */
import fs from 'node:fs';
import path from 'node:path';
import { execSync } from 'node:child_process';
import { fileURLToPath } from 'node:url';

const ROOT = path.resolve(path.dirname(fileURLToPath(import.meta.url)), '..');
const fails = [];
let passes = 0;
function pass(msg) { passes++; console.log('PASS ' + msg); }
function fail(msg) { fails.push(msg); console.log('FAIL ' + msg); }

  try {
    if (!fs.existsSync(path.join(ROOT, 'db/migrations/20260704_create_music_students.sql'))) { fail('Missing: db/migrations/20260704_create_music_students.sql'); }
    else { const c = fs.readFileSync(path.join(ROOT, 'db/migrations/20260704_create_music_students.sql'), 'utf8'); /CREATE\s+TABLE/i.test(c) ? pass('db/migrations/20260704_create_music_students.sql exists + has CREATE TABLE') : fail('db/migrations/20260704_create_music_students.sql exists but no CREATE TABLE'); }
  } catch (e) { fail('db/migrations/20260704_create_music_students.sql: ' + e.message.split('\n')[0]); }
  try {
    if (!fs.existsSync(path.join(ROOT, 'db/migrations/20260704_create_music_teachers.sql'))) { fail('Missing: db/migrations/20260704_create_music_teachers.sql'); }
    else { const c = fs.readFileSync(path.join(ROOT, 'db/migrations/20260704_create_music_teachers.sql'), 'utf8'); /CREATE\s+TABLE/i.test(c) ? pass('db/migrations/20260704_create_music_teachers.sql exists + has CREATE TABLE') : fail('db/migrations/20260704_create_music_teachers.sql exists but no CREATE TABLE'); }
  } catch (e) { fail('db/migrations/20260704_create_music_teachers.sql: ' + e.message.split('\n')[0]); }
  try {
    if (!fs.existsSync(path.join(ROOT, 'db/migrations/20260704_create_music_practice_sessions.sql'))) { fail('Missing: db/migrations/20260704_create_music_practice_sessions.sql'); }
    else { const c = fs.readFileSync(path.join(ROOT, 'db/migrations/20260704_create_music_practice_sessions.sql'), 'utf8'); /CREATE\s+TABLE/i.test(c) ? pass('db/migrations/20260704_create_music_practice_sessions.sql exists + has CREATE TABLE') : fail('db/migrations/20260704_create_music_practice_sessions.sql exists but no CREATE TABLE'); }
  } catch (e) { fail('db/migrations/20260704_create_music_practice_sessions.sql: ' + e.message.split('\n')[0]); }
  try {
    if (!fs.existsSync(path.join(ROOT, 'db/migrations/20260704_create_music_talent_evaluations.sql'))) { fail('Missing: db/migrations/20260704_create_music_talent_evaluations.sql'); }
    else { const c = fs.readFileSync(path.join(ROOT, 'db/migrations/20260704_create_music_talent_evaluations.sql'), 'utf8'); /CREATE\s+TABLE/i.test(c) ? pass('db/migrations/20260704_create_music_talent_evaluations.sql exists + has CREATE TABLE') : fail('db/migrations/20260704_create_music_talent_evaluations.sql exists but no CREATE TABLE'); }
  } catch (e) { fail('db/migrations/20260704_create_music_talent_evaluations.sql: ' + e.message.split('\n')[0]); }
  try {
    if (!fs.existsSync(path.join(ROOT, 'services/ai-listening-coach.js'))) { fail('Missing: services/ai-listening-coach.js'); }
    else { execSync(`node -c "${path.join(ROOT, 'services/ai-listening-coach.js')}"`, { encoding: 'utf8', stdio: 'pipe' }); pass('services/ai-listening-coach.js exists + syntax OK'); }
  } catch (e) { fail('services/ai-listening-coach.js: ' + e.message.split('\n')[0]); }
  try {
    if (!fs.existsSync(path.join(ROOT, 'routes/music-talent-routes.js'))) { fail('Missing: routes/music-talent-routes.js'); }
    else { execSync(`node -c "${path.join(ROOT, 'routes/music-talent-routes.js')}"`, { encoding: 'utf8', stdio: 'pipe' }); pass('routes/music-talent-routes.js exists + syntax OK'); }
  } catch (e) { fail('routes/music-talent-routes.js: ' + e.message.split('\n')[0]); }
  try {
    if (!fs.existsSync(path.join(ROOT, 'public/overlay/student-dashboard.html'))) { fail('Missing: public/overlay/student-dashboard.html'); }
    else { const c = fs.readFileSync(path.join(ROOT, 'public/overlay/student-dashboard.html'), 'utf8'); c.length > 50 ? pass('public/overlay/student-dashboard.html exists (' + c.length + ' bytes)') : fail('public/overlay/student-dashboard.html too small'); }
  } catch (e) { fail('public/overlay/student-dashboard.html: ' + e.message.split('\n')[0]); }

console.log('\nResults: ' + passes + ' passed, ' + fails.length + ' failed of 7 files');
if (fails.length) {
  console.error('FAILURES:', fails.join('; '));
  process.exit(1);
}
console.log('ALL CHECKS PASSED');
process.exit(0);
