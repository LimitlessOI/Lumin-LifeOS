# STOP — Hist / legacy boundary (mandatory before any edit)

**You must read this before choosing a path.** Do not skip because it is “just docs.”

---

## One screen (memorize)

| Build **new** factory / BuilderOS work | **`factory-staging/`** + **`builderos-reboot/MISSIONS/`** |
| Run **live** LifeOS on Railway today | **Production spine** — `routes/`, `services/`, `startup/`, `public/overlay/` |
| Touch **old** code, experiment folders, retired providers | **Hist only** — read, salvage, cite. **Do not extend.** |

**Legacy ≠ deleted.** Legacy = **Hist-owned historical record.** Adam locked this **2026-05-24**.

---

## Hard rules

1. **Do not extend legacy** as the default shortcut (no “faster in Cursor” on Hist paths).
2. **Do not treat** experiment buckets (`src/`, root `frontend/`, `my-project/`, nested `services/*/` packages, `.claude/worktrees/`, etc.) as active product — see registry **HIST-BUCKET-***.
3. **Do not use** OpenRouter or Together — **retired** (`HIST-PROV-*`). Claude = direct **`ANTHROPIC_API_KEY`** → `claude_sonnet`.
4. **Before editing any Hist-tagged path**, state a **Hist mandatory case** (template in registry §4).
5. **Salvage good ideas** → mission + `factory-staging/`, not copy-paste into legacy spine.

---

## Full map (when you need IDs and paths)

`docs/architecture/HIST_LEGACY_SYSTEM_REGISTRY.md`

Layer model (factory vs spine): `prompts/00-SYSTEM-AUTHORITY-LAYERS.md`

---

## If Adam did not name a path

```text
Factory / mission / execute-step?  → factory-staging/
Live product / deploy / overlay?     → production spine (builder-first)
Old file / “what was this?”        → Hist registry → read_only or HALT
```

**Violation:** shipping new behavior only in a Hist bucket without receipt = **§2.6 drift**.
