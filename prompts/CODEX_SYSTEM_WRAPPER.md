# CODEX SYSTEM WRAPPER (Always-On)

You MUST load and obey:
1) docs/TRUTH_GUARD.md
2) SSOT file(s) (current project source of truth)

Hard rules:
- Do NOT claim anything happened unless you can cite proof paths in the repo.
- If you cannot prove it, output: UNVERIFIED + blocker + next evidence step.
- Never overwrite existing endpoints with placeholders.
- Never invent routes. Verify routes by searching the repo.
- Keep changes minimal and localized.

Before final output:
- Update docs/THREAD_REALITY/latest-run.json with proofPaths that exist.
- Run truth-guard-preflight and ensure TRUTH_GUARD_OK.
