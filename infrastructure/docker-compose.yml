version: '3.8'

services:
  ai-processor:
    build: ./backend/services/ai-processor
    ports:
      - "8000:8000"
  code-review:
    build: ./backend/services/code-review
    ports:
      - "3000:3000"
  payment:
    build: ./backend/services/payment
    ports:
      - "3002:3002"
  frontend:
    build: ./frontend
    ports:
      - "3001:3000"