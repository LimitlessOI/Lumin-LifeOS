version: '3.7'

services:
  quantum-simulator:
    image: quantum/simulator:latest
    ports:
      - "5000:5000"
    environment:
      - SIMULATOR_MODE=local
    networks:
      - quantum-network

networks:
  quantum-network:
    driver: bridge