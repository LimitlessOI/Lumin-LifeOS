{
  "schema": "product_build_queue_v1",
  "product_id": "site-builder",
  "verify_script": "scripts/verify-site-builder-lane.mjs",
  "notes": "Structured, ordered build steps the autonomous product-build orchestrator executes one at a time via the /build primitive. Each step is a single-file, well-specified task with a concrete acceptance. Free-form prose build plans live in PRODUCT_HOME; THIS file is the machine-executable queue.",
  "updated_at": "2026-07-06b",
  "steps": [
    {
      "id": "sb-ab-subject-testing",
      "status": "done",
      "target_file": "services/site-builder-email-templates.js",
      "task": "Add A/B subject-line testing to cold outreach: pickSubjectVariant(prospect, variants) that deterministically assigns each prospect to one of N subject variants (hash of prospect id) and records the assigned variant so opens/replies can be attributed per variant.",
      "spec": "Pure function, no network. Export pickSubjectVariant(prospectId, variants[]) -> { variant, index }. Deterministic by hashing prospectId. Add a DEFAULT_SUBJECT_VARIANTS array. Do not change existing exported template functions' signatures.",
      "depends_on": [],
      "founder_gated": false,
      "attempts": 1,
      "commit_sha": "9cfc4e591748a06b2aa3a083b72645b2be0c068f",
      "proof": "Built autonomously via /build primitive; verified deterministic FNV-1a assignment, near-uniform distribution (996/995/1009 across 3000 ids), input validation, custom-variant support."
    },
    {
      "id": "sb-preview-expiry-sweep",
      "status": "done",
      "target_file": "scripts/site-builder-preview-expiry-cron.mjs",
      "task": "Preview-expiry sweep: delete preview directories older than PREVIEW_TTL_DAYS (default 30) under public/previews and mark the matching prospect_sites row expired, printing a JSON summary. Fail-closed: never delete outside public/previews.",
      "spec": "Node ESM CLI script runnable via `node scripts/site-builder-preview-expiry-cron.mjs`; it ends with a main() call and process.exit. CRITICAL GATE-SAFETY CONSTRAINTS (must all hold): (1) PREVIEW_TTL_DAYS defaults to 30 when the env var is unset or blank — never throw for a missing env var. (2) Do NOT import any database client (e.g. 'pg') at module top level — the only allowed top-level imports are node builtins (fs, path, url, os). If DB access is needed, use a dynamic `await import('pg')` INSIDE the apply branch only. (3) SAFE BY DEFAULT: the script runs as a DRY RUN unless BOTH process.env.SWEEP_APPLY==='1' AND process.env.DATABASE_URL is set. In dry-run mode it must delete NOTHING and touch no database — it only scans public/previews (if present), and always prints a single-line JSON summary { mode, scanned, expired, deleted, skipped } to stdout and exits 0. (4) Resolve preview root as path.resolve(process.cwd(),'public/previews'); before every unlink, verify the resolved target path startsWith(root + path.sep); skip anything failing that guard and count it in skipped. (5) Always print the JSON summary to stdout and exit 0 on success (non-zero only on an unexpected internal error). Keep it a single self-contained file.",
      "depends_on": [],
      "founder_gated": false,
      "attempts": 1,
      "commit_sha": "02f2e809c",
      "proof": "Built autonomously by the never-stop loop after activation on prod (lumin-web service). 227-line dry-run-safe script: TTL validation/defaults, SWEEP_APPLY gate, path-traversal guard, JSON summary. node --check passes."
    },
    {
      "id": "sb-editor-sidebar",
      "status": "done",
      "target_file": "services/site-builder-editor-sidebar.js",
      "task": "Render the LEFT pane of the editor workspace: the services/upsell sidebar. Export renderSidebar({ services, clientId, baseUrl }) that returns an HTML STRING FRAGMENT (not a full document) for a vertical list built from services[] (each { id, name, blurb, price, cadence, recommended }). Services flagged recommended show a small 'Recommended' badge and sort first. Clicking a service toggles a 'selected' highlight, adds/removes it from a running 'Your plan' summary at the bottom of the sidebar, and fires a best-effort beacon (navigator.sendBeacon, fall back to fetch with keepalive) to `${baseUrl}/api/v1/sites/select-service?id=<clientId>&service=<id>`. Include a small inline <script> that wires the click behavior via event delegation on the fragment's root element.",
      "spec": "Single self-contained ESM module, ZERO import-time side effects, no network at import, no DB, no project imports. Named export renderSidebar + default export. Returns a STRING (an HTML fragment, NOT a full <html> document). Define a local htmlEscape() and escape name/blurb/price/cadence before interpolation. Guard services as possibly undefined -> default []. The inline script must be wrapped in try/catch and never throw if elements are missing. Deterministic given the same input ordering. @ssot docs/products/site-builder/PRODUCT_HOME.md.",
      "depends_on": [],
      "founder_gated": false,
      "attempts": 1,
      "max_output_tokens": 7168,
      "last_attempt_at": "2026-07-07T04:21:19.761Z",
      "last_error": "no_commit_sha (claimed pass without proof — treated as failure)",
      "revive_count": 4,
      "revived_at": "2026-07-07T04:21:19.761Z",
      "commit_sha": "8d71ee09344d9be598bd3fb8e2aa7058e409006c",
      "completed_at": "2026-07-07T04:21:21.414Z",
      "deploy_proven": true
    },
    {
      "id": "sb-editor-canvas",
      "status": "done",
      "target_file": "services/site-builder-editor-canvas.js",
      "task": "Render the CENTER pane of the editor workspace: the live site canvas. Export renderCanvas({ siteFile, variants, palettes, clientId, editToken, baseUrl }) returning an HTML STRING FRAGMENT containing: (1) a top toolbar with template-switcher chips built from variants[] (each {id,name,file}) that set the iframe src, palette swatches from palettes[] (each {name,primary,accent}) that POST a recolor instruction to `${baseUrl}/api/v1/sites/edit`, a mobile/desktop device toggle that sets the iframe width, and a Save button that POSTs the iframe document's serialized outerHTML to `${baseUrl}/api/v1/sites/save-edits` as { clientId, file, html, token }; (2) a same-origin <iframe> loading siteFile; (3) an inline <script> giving click-to-edit (clicking a text node in the iframe makes it contentEditable) and per-top-level-section move-up / move-down / hide controls operating only on iframe.contentDocument top-level children.",
      "spec": "Single self-contained ESM module, ZERO import-time side effects, no project imports. Named export renderCanvas + default export. Returns a STRING (HTML fragment, NOT a full document). Define local htmlEscape(); escape siteFile and all variant/palette text. Every fetch includes the editToken (from a JS const) and on non-2xx shows an inline error — never a silent/blank failure. All iframe DOM access wrapped in try/catch guarding cross-origin/null contentDocument; never throw. Guard variants/palettes as possibly undefined -> []. @ssot docs/products/site-builder/PRODUCT_HOME.md.",
      "depends_on": [],
      "founder_gated": false,
      "attempts": 3,
      "max_output_tokens": 12288,
      "last_attempt_at": "2026-07-07T04:25:36.016Z",
      "last_error": "deploy_serves_different_sha",
      "revive_count": 2,
      "revived_at": "2026-07-07T03:49:39.166Z",
      "commit_sha": "afeda3304c3d569b2510df1a618da467095c4e9f",
      "completed_at": "2026-07-07T04:25:37.234Z",
      "deploy_proven": true
    },
    {
      "id": "sb-editor-chat",
      "status": "done",
      "target_file": "services/site-builder-editor-chat.js",
      "task": "Render the RIGHT pane of the editor workspace: the AI chat + voice panel. Export renderChatPanel({ clientId, editToken, baseUrl }) returning an HTML STRING FRAGMENT with a scrollable message list, a text input, a Send button, and a microphone button. Sending (typed or spoken) appends the user's message to the list and POSTs { clientId, file: null, instruction, token } to `${baseUrl}/api/v1/sites/edit`; on success it appends a confirmation and dispatches a window CustomEvent 'sb-editor:reload' so the canvas can reload its iframe. Include a short helper hint line telling the client they can type or speak what they want changed.",
      "spec": "Single self-contained ESM module, ZERO import-time side effects, no project imports. Named export renderChatPanel + default export. Returns a STRING (HTML fragment). Microphone uses window.webkitSpeechRecognition||window.SpeechRecognition; if unavailable, hide the mic button and show a tooltip 'Voice not supported in this browser' — never throw. Every fetch includes editToken; non-2xx shows an inline error bubble, never blank. Inline script wrapped in try/catch. Define local htmlEscape(). @ssot docs/products/site-builder/PRODUCT_HOME.md.",
      "depends_on": [],
      "founder_gated": false,
      "attempts": 2,
      "max_output_tokens": 8192,
      "last_attempt_at": "2026-07-07T04:30:21.895Z",
      "last_error": "no_commit_sha (claimed pass without proof — treated as failure)",
      "revive_count": 1,
      "revived_at": "2026-07-07T02:42:10.293Z",
      "commit_sha": "9bf23be78a8c9645598a8b50d2e024c7ce9e6d07",
      "completed_at": "2026-07-07T04:30:23.536Z",
      "deploy_proven": true
    },
    {
      "id": "sb-editor-strategy",
      "status": "done",
      "target_file": "services/site-builder-editor-strategy.js",
      "task": "Render the competitor STRATEGY view for the editor. Export renderStrategyPanel({ strategy }) returning an HTML STRING FRAGMENT that shows strategy.synopsis (a plain-English paragraph) followed by strategy.competitors[] (each { name, category, score, strengths, weaknesses }) rendered as 1–10 scorecards: the numeric score (0–10) as a labeled bar, category tag, and short strengths/weaknesses lists. Group scorecards by category so midwifery and wellness competitors read as separate lanes. If strategy or competitors is missing/empty, render a friendly 'Competitor analysis will appear here once your benchmark runs' placeholder.",
      "spec": "Single self-contained ESM module, ZERO import-time side effects, no network, no DB, no project imports. Named export renderStrategyPanel + default export. Returns a STRING (HTML fragment). Define local htmlEscape() and escape ALL strategy text (synopsis, names, strengths, weaknesses) before interpolation. Clamp score into 0–10 for the bar width. Guard every field as possibly undefined. Never fabricate competitors or scores — only render what is provided. Deterministic output. @ssot docs/products/site-builder/PRODUCT_HOME.md.",
      "depends_on": [],
      "founder_gated": false,
      "attempts": 1,
      "max_output_tokens": 7168,
      "last_attempt_at": "2026-07-07T04:40:03.362Z",
      "last_error": "no_commit_sha (claimed pass without proof — treated as failure)",
      "revive_count": 1,
      "revived_at": "2026-07-07T02:42:10.293Z",
      "commit_sha": "b3ad9358a39e178e14036d7d4fad581b51d5d280",
      "completed_at": "2026-07-07T04:40:04.521Z",
      "deploy_proven": true
    },
    {
      "id": "sb-editor-shell",
      "status": "pending",
      "target_file": "services/site-builder-editor.js",
      "task": "Compose the full three-pane editor workspace document from the already-built pane modules. Import renderSidebar from './site-builder-editor-sidebar.js', renderCanvas from './site-builder-editor-canvas.js', renderChatPanel from './site-builder-editor-chat.js', and renderStrategyPanel from './site-builder-editor-strategy.js'. Export renderEditorShell({ businessName, clientId, siteFile, variants, palettes, editToken, baseUrl, strategy, services }) that returns ONE complete HTML document: a top bar showing the business name, then a THREE-COLUMN layout — LEFT = renderSidebar({ services, clientId, baseUrl }), CENTER = renderCanvas({ siteFile, variants, palettes, clientId, editToken, baseUrl }), RIGHT = a right column containing renderChatPanel({ clientId, editToken, baseUrl }) above a collapsible 'Strategy' section holding renderStrategyPanel({ strategy }). Add the reload behaviour as a literal <script>...</script> string EMBEDDED INSIDE the returned HTML document (part of the template literal) — NOT as top-level module code. CRITICAL: this file is imported by Node on the server, so it MUST have ZERO top-level references to window, document, or any browser global; ALL client-side JavaScript (event listeners, iframe reload, etc.) belongs inside the returned HTML string within a <script> tag. Top-level browser code crashes Node on import and fails the Railway healthcheck. This module is ONLY composition + document scaffold — it must not re-implement pane internals.",
      "spec": "Single self-contained ESM module. It MAY import the four sibling pane modules by relative path (they are committed before this step via depends_on). Named export renderEditorShell + default export. Returns a full HTML document using Tailwind CDN (https://cdn.tailwindcss.com) and Alpine.js CDN (https://unpkg.com/alpinejs@3.x.x/dist/cdn.min.js, defer). Define a local htmlEscape() and escape businessName before interpolation. Guard every destructured field (default services/variants/palettes/strategy sensibly). No DB, no network, and NO top-level window/document/browser-global access at import time — any client-side behaviour must live only inside the returned HTML string as <script> content (server imports this module; top-level browser globals crash Node). Keep this file small — delegate all pane markup to the imported render functions. @ssot docs/products/site-builder/PRODUCT_HOME.md.",
      "depends_on": [
        "sb-editor-sidebar",
        "sb-editor-canvas",
        "sb-editor-chat",
        "sb-editor-strategy"
      ],
      "founder_gated": false,
      "attempts": 2,
      "max_output_tokens": 8192,
      "last_attempt_at": "2026-07-07T05:50:10.649Z",
      "last_error": "no_commit_sha (claimed pass without proof — treated as failure)",
      "revive_count": 1,
      "revived_at": "2026-07-07T05:24:18.589Z"
    },
    {
      "id": "sb-editor-endpoints",
      "status": "pending",
      "target_file": "routes/site-builder-editor-routes.js",
      "task": "New Express route module exporting createSiteBuilderEditorRoutes(app, { callCouncilMember, baseUrl }) that mounts three POST endpoints under /api/v1/sites: `/edit` (AI edit), `/save-edits` (manual save), and `/revert` (restore the most recent .bak). All three authenticate with a PER-PREVIEW token (NOT the global command key): read public/previews/<clientId>/meta.json and require body.token === meta.editToken, else respond 403. `/edit` reads the target file (body.file, relative, under the client's preview dir), calls callCouncilMember with a STRONG paid model, passing the current HTML + the user's instruction + strict anti-fabrication TRUTH rules, asking it to return the ENTIRE modified HTML document; validate the response contains '<html'; write a timestamped .bak of the old file then overwrite; respond { ok:true }. `/save-edits` validates body.html contains '<html', writes a .bak then overwrites body.file. `/revert` restores the newest .bak for body.file.",
      "spec": "Single self-contained ESM file. Named export createSiteBuilderEditorRoutes + default. PATH SAFETY (hard, fail-closed): resolve previewsRoot = path.resolve(process.cwd(),'public/previews') ONCE; for every file access resolve the candidate and require resolved.startsWith(previewsRoot + path.sep) AND clientId matches /^[\\w-]+$/ AND file has no '..' — otherwise 403. Never read/write outside previewsRoot. Token check fails closed when meta.json is missing, unreadable, or has no editToken. No database. Use express.json() body parsing already present. Wrap AI/file errors and respond JSON { ok:false, error } with appropriate status; never leak absolute paths in errors. TRUTH rules passed to the model must forbid inventing prices, stats, ratings, review counts, and named testimonials. @ssot docs/products/site-builder/PRODUCT_HOME.md.",
      "depends_on": [
        "sb-editor-shell"
      ],
      "founder_gated": false,
      "attempts": 0
    },
    {
      "id": "sb-competitor-categories",
      "status": "done",
      "target_file": "services/site-builder-competitor-categories.js",
      "task": "Create a helper that classifies a wellness business into ALL of its practice categories (a business can be several at once — e.g. midwife AND general wellness) and returns per-category competitor search queries. Export classifyPracticeCategories(businessInfo) -> { categories: [{ id, label, searchQueries[] }] } covering at least 'midwifery' (home birth, midwife, doula, birth support) and 'wellness' (holistic wellness, wellness practitioner) detection from businessInfo.industry/services/keywords, and buildDualCategoryQueries(businessInfo) that always returns queries for BOTH midwifery and wellness when either is detected, so the benchmark compares against both lanes.",
      "spec": "Single self-contained ESM module, pure functions, no network/DB/imports. Named exports classifyPracticeCategories and buildDualCategoryQueries + default export object. Case-insensitive keyword matching over arrays that may be undefined (guard everything). Deterministic output. Never throw on missing fields. @ssot docs/products/site-builder/PRODUCT_HOME.md.",
      "depends_on": [],
      "founder_gated": false,
      "attempts": 1,
      "commit_sha": "6cce97ef3",
      "proof": "Built autonomously by the never-stop loop once the prod GitHub token was restored; committed as services/site-builder-competitor-categories.js. node --check passes."
    },
    {
      "id": "sb-social-discovery",
      "status": "done",
      "target_file": "services/site-builder-social-discovery.js",
      "task": "Create a social-media discovery helper that finds a business's social profiles — especially YouTube — even when not linked on their website. Export async discoverSocials(businessInfo, { fetchImpl }) that (1) collects any social URLs already present in businessInfo, and (2) builds candidate handles from the business name and, when a fetchImpl is provided, verifies likely YouTube (youtube.com/@handle), Instagram, and Facebook URLs by HEAD/GET and returns only reachable ones. Also export extractYouTubeChannelId(html) to pull a channelId from a YouTube page's HTML. Returns { youtube: {url,channelId}|null, instagram, facebook, tiktok, others[] }.",
      "spec": "Single self-contained ESM module. Named exports discoverSocials, extractYouTubeChannelId + default. NETWORK IS OPTIONAL AND INJECTED: never import a network library; use the provided fetchImpl (default to globalThis.fetch) and if it is unavailable, return only the socials already known from businessInfo (fail-open to no-op, never throw). Wrap every fetch in try/catch with a short timeout via AbortController. Do not fabricate a profile — only return URLs actually confirmed reachable or already provided. Guard all businessInfo fields. @ssot docs/products/site-builder/PRODUCT_HOME.md.",
      "depends_on": [],
      "founder_gated": false,
      "attempts": 1,
      "last_attempt_at": "2026-07-07T05:14:44.738Z",
      "last_error": "no_commit_sha (claimed pass without proof — treated as failure)",
      "revive_count": 1,
      "revived_at": "2026-07-07T02:53:53.254Z",
      "commit_sha": "108ab3ccbfb5f3aed44b1c763653151a3b80aee5",
      "completed_at": "2026-07-07T05:14:45.941Z",
      "deploy_proven": true
    },
    {
      "id": "sb-service-catalog",
      "status": "pending",
      "target_file": "services/site-builder-service-catalog.js",
      "task": "Create the à-la-carte add-on SERVICE CATALOG that powers the editor's left sidebar. Export SERVICE_CATALOG (array of { id, name, blurb, price, cadence }) covering: logo & brand kit, corporate/brand package, ClickFunnels build + ad placement/management, managed Google Business Profile + local SEO, social-media management, and an SEO/content care plan — with honest, GOAL-based (never GUARANTEE) copy. For the Google offering use exactly this framing: 'managed Google Business Profile + local SEO with the GOAL of ranking on the first page of results for your category' — a goal/minimum-expectation, NEVER a guarantee and NEVER 'guaranteed #1'. Each service may carry an optional `results` proof object ({ headline, sampleSize, onFirstPage, atNumberOne }) — real, MEASURED client outcomes only (e.g. '11 of 15 managed businesses rank on page one, 9 at #1'); export formatResultsProof(results) that returns a short honest string ONLY when results.sampleSize>0, and returns null (hide the block) otherwise — NEVER fabricate outcome numbers. Export recommendServices(strategy) that, given the competitor strategy object ({ competitors:[{doesPoorly/weaknesses}], gaps }), returns the SAME catalog with a boolean `recommended` flag set on the services that address the biggest competitor gaps (e.g. flag ad-management/funnel when competitors lack ads, flag video/social when competitors lack video). Prices come from the product revenue model, not invented.",
      "spec": "Single self-contained ESM module, pure data + pure functions, no network/DB/imports/side effects. Named exports SERVICE_CATALOG, recommendServices, formatResultsProof + default. recommendServices must never throw on missing/partial strategy (guard everything, default to no recommendations). Copy must comply with the product's TRUTH non-negotiables: no guaranteed rankings, no fabricated outcomes. Deterministic output. @ssot docs/products/site-builder/PRODUCT_HOME.md.",
      "depends_on": [],
      "founder_gated": false,
      "attempts": 0
    }
  ]
}
