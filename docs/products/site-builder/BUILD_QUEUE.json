{
  "schema": "product_build_queue_v1",
  "product_id": "site-builder",
  "verify_script": "scripts/sentry-site-builder-prealpha-gate.mjs",
  "notes": "Structured, ordered build steps the autonomous product-build orchestrator executes one at a time via the /build primitive. Each step is a single-file, well-specified task with a concrete acceptance. Free-form prose build plans live in PRODUCT_HOME; THIS file is the machine-executable queue. COMPLETION GATE (SO-002): verify_script runs the SENTRY two-layer pre-alpha gate — Layer A (structural HTTP) always, Layer B (human-sim real browser + UX critique) whenever prod creds are present — and fails closed. No step is 'done' until SENTRY passes as a real client. Prior lane check (scripts/verify-site-builder-lane.mjs) still runnable via `npm run verify:site-builder`.",
  "updated_at": "2026-07-08",
  "steps": [
    {
      "id": "sb-deliverability-gate",
      "status": "skipped",
      "target_file": "services/site-builder-prospect-runner.js",
      "task": "FOUNDER DECISION (2026-07-21): Postmark cannot be used for cold outreach -- its AUP does not permit unsolicited/cold email sending. This supersedes the original Postmark-API-based approach (9 straight failed attempts, all the same missing-export bug). New approach: a vendor-neutral deliverability check using standard DNS SPF/DKIM records, which works regardless of which sending provider (Amazon SES, Mailgun, SMTP2GO, etc.) is eventually chosen for actual cold-email sending.",
      "spec": "Insert EXACTLY this code block, verbatim, character for character, immediately BEFORE the line that reads `export default {` in services/site-builder-prospect-runner.js (that line currently begins the file's closing default-export object listing createProspectClientId, enqueueProspectJob, etc.). Do NOT edit, add to, or remove anything from the `export default { ... }` object itself -- leave it byte-for-byte as-is. Do NOT add checkDomainDeliverabilityDNS as a property inside that object -- its export must come ONLY from the `export async function` declaration below, not from being merely named inside the default-export object (that distinction is what caused 9 straight failures on the prior, unrelated Postmark-based version of this step -- this is a fresh function name and fresh approach, but the same file-structure rule applies).\n\nAdd this import at the very top of the file, immediately after the existing `import logger from './logger.js';` line: `import dns from 'node:dns';`\n\nInsert this exact function block immediately before `export default {`:\n\nexport async function checkDomainDeliverabilityDNS(domain, { dkimSelector = 'default', resolveTxtFn } = {}) {\n  const resolve = resolveTxtFn || ((name) => dns.promises.resolveTxt(name));\n  if (!domain) {\n    return { deliverabilityReady: false, deliverabilityBlockers: ['missing_domain'] };\n  }\n  const blockers = [];\n  let spfFound = false;\n  let dkimFound = false;\n  try {\n    const spfRecords = await resolve(domain);\n    spfFound = spfRecords.some((rec) => rec.join('').toLowerCase().includes('v=spf1'));\n  } catch (err) {\n    blockers.push(`spf_lookup_failed:${err.code || err.message}`);\n  }\n  if (!spfFound) blockers.push('spf_record_not_found');\n  try {\n    const dkimRecords = await resolve(`${dkimSelector}._domainkey.${domain}`);\n    dkimFound = dkimRecords.some((rec) => rec.join('').toLowerCase().includes('v=dkim1'));\n  } catch (err) {\n    blockers.push(`dkim_lookup_failed:${err.code || err.message}`);\n  }\n  if (!dkimFound) blockers.push('dkim_record_not_found');\n  return {\n    deliverabilityReady: spfFound && dkimFound,\n    deliverabilityBlockers: blockers,\n  };\n}\n\nThis is the ENTIRE required change for this step -- do not modify evaluateSiteBuilderEmailReadiness, enqueueProspectJob, or any other function, and do not add, remove, or reference Postmark anywhere (this function is intentionally provider-agnostic: it checks the sending domain's own public DNS records, which is identical regardless of which ESP -- Amazon SES, Mailgun, SMTP2GO, or anything else -- is used to actually send). resolveTxtFn is dependency-injected so tests can supply a fake DNS resolver instead of making real network calls; it defaults to a real node:dns lookup when not supplied. Must fail closed (deliverabilityReady: false, with a specific blocker string) on any DNS lookup error or missing record -- never fail open, never throw out of this function.",
      "depends_on": [],
      "founder_gated": false,
      "attempts": 1,
      "priority": "high",
      "source": "chair_architect_competitive_review",
      "finding_note": "2026-07-19 competitive review: every DFY cold-outreach competitor researched (OutreachBloom, doneforyou.com) treats domain warming/deliverability verification as foundational before real send volume; this blueprint jumps straight from env-var setup to sending real prospects with no deliverability check at all.",
      "blueprint_id": "PRODUCT-SITE-BUILDER-BUILD-QUEUE-TWIN-V1",
      "blueprint_step_id": "sb-deliverability-gate",
      "mission_id": "PRODUCT-site-builder",
      "expected_exports": [
        "checkDomainDeliverabilityDNS"
      ],
      "behavior_assertions": [
        {
          "type": "exports_smoke",
          "path": "services/site-builder-prospect-runner.js",
          "exports": [
            "checkDomainDeliverabilityDNS"
          ],
          "assertion_id": "expected_exports:services/site-builder-prospect-runner.js"
        }
      ],
      "last_attempt_at": "2026-07-21T20:04:28.392Z",
      "last_error": "SENTRY_FAILED: behavior_proof; behavior_assertion: missing_exports:checkDomainDeliverabilityDNS; behavior_assertion_failed; behavior_proof",
      "commit_sha": null,
      "built_sha": null,
      "proof": null,
      "failure_signature": "SENTRY_FAILED: behavior_proof; behavior_assertion: missing_exports:checkDomainDeliverabilityDNS; behavior_assertion_failed; behavior_proof",
      "same_signature_count": 1,
      "escalation_class": "confirmed_platform_bug",
      "revive_count": 8,
      "revived_at": "2026-07-21T19:57:21.036Z",
      "last_attempt": null,
      "demoted": true,
      "demote_reason": "revive_exhausted:SENTRY_FAILED: behavior_proof; behavior_assertion: missing_exports:checkDomainDeliverabilityDNS; behavior_assertion_failed; behavior_proof",
      "demoted_at": "2026-07-21T20:20:19.240Z",
      "park_until": null,
      "no_op": null,
      "pre_existing": null,
      "blocker_class": null,
      "claim_level": null,
      "blocker_type": null,
      "blocker_resolution": null,
      "model_rotation": 1,
      "force_model_rotation": false,
      "founder_alerted_signature": null,
      "hard_stopped": false,
      "unsolved": false,
      "revive_note": "STOP retrying identically (10 consecutive failures across two completely different approaches -- Postmark-based checkPostmarkDomainVerified x7-8, DNS-based checkDomainDeliverabilityDNS x2). Every single attempt shows the identical split: file_contains passes (the function name string exists somewhere in the file) but exports_smoke fails with missing_exports (no real top-level export statement). This ruled out spec-wording as the cause -- literal verbatim code with an explicit anchor line failed identically to prose instructions, and a brand-new unrelated function name failed identically to the original. Root cause is very likely in the BUILDER/PATCHING layer, not codegen output: something about applying a new-function insertion to a file that ends in an export default {...} object literal (services/site-builder-prospect-runner.js has one) appears to reliably relocate or drop the inline export keyword. Needs investigation in factory-staging/factory-core/builder/run-step.js or wherever file-patch application happens for greenfield edits to existing files -- NOT another spec rewrite. Recommend: (1) test this theory on a throwaway file that also ends in an export default {} object literal, (2) if confirmed, either fix the patch-application logic or change this step to target a NEW file (e.g. services/site-builder-deliverability-check.js, exported and imported into site-builder-prospect-runner.js) as a workaround that avoids the trigger condition entirely. SUPERSEDED by sb-deliverability-gate-v2, which targets a brand-new file to avoid the trigger condition."
    },
    {
      "id": "sb-ab-subject-testing",
      "status": "done",
      "target_file": "services/site-builder-email-templates.js",
      "task": "Add A/B subject-line testing to cold outreach: pickSubjectVariant(prospect, variants) that deterministically assigns each prospect to one of N subject variants (hash of prospect id) and records the assigned variant so opens/replies can be attributed per variant.",
      "spec": "Pure function, no network. Export pickSubjectVariant(prospectId, variants[]) -> { variant, index }. Deterministic by hashing prospectId. Add a DEFAULT_SUBJECT_VARIANTS array. Do not change existing exported template functions' signatures.",
      "depends_on": [],
      "founder_gated": false,
      "attempts": 1,
      "commit_sha": "9cfc4e591748a06b2aa3a083b72645b2be0c068f",
      "proof": "Built autonomously via /build primitive; verified deterministic FNV-1a assignment, near-uniform distribution (996/995/1009 across 3000 ids), input validation, custom-variant support.",
      "blueprint_id": "PRODUCT-SITE-BUILDER-BUILD-QUEUE-TWIN-V1",
      "blueprint_step_id": "sb-ab-subject-testing",
      "mission_id": "PRODUCT-site-builder"
    },
    {
      "id": "sb-preview-expiry-sweep",
      "status": "done",
      "target_file": "scripts/site-builder-preview-expiry-cron.mjs",
      "task": "Preview-expiry sweep: delete preview directories older than PREVIEW_TTL_DAYS (default 30) under public/previews and mark the matching prospect_sites row expired, printing a JSON summary. Fail-closed: never delete outside public/previews.",
      "spec": "Node ESM CLI script runnable via `node scripts/site-builder-preview-expiry-cron.mjs`; it ends with a main() call and process.exit. CRITICAL GATE-SAFETY CONSTRAINTS (must all hold): (1) PREVIEW_TTL_DAYS defaults to 30 when the env var is unset or blank — never throw for a missing env var. (2) Do NOT import any database client (e.g. 'pg') at module top level — the only allowed top-level imports are node builtins (fs, path, url, os). If DB access is needed, use a dynamic `await import('pg')` INSIDE the apply branch only. (3) SAFE BY DEFAULT: the script runs as a DRY RUN unless BOTH process.env.SWEEP_APPLY==='1' AND process.env.DATABASE_URL is set. In dry-run mode it must delete NOTHING and touch no database — it only scans public/previews (if present), and always prints a single-line JSON summary { mode, scanned, expired, deleted, skipped } to stdout and exits 0. (4) Resolve preview root as path.resolve(process.cwd(),'public/previews'); before every unlink, verify the resolved target path startsWith(root + path.sep); skip anything failing that guard and count it in skipped. (5) Always print the JSON summary to stdout and exit 0 on success (non-zero only on an unexpected internal error). Keep it a single self-contained file.",
      "depends_on": [],
      "founder_gated": false,
      "attempts": 1,
      "commit_sha": "02f2e809c",
      "proof": "Built autonomously by the never-stop loop after activation on prod (lumin-web service). 227-line dry-run-safe script: TTL validation/defaults, SWEEP_APPLY gate, path-traversal guard, JSON summary. node --check passes.",
      "blueprint_id": "PRODUCT-SITE-BUILDER-BUILD-QUEUE-TWIN-V1",
      "blueprint_step_id": "sb-preview-expiry-sweep",
      "mission_id": "PRODUCT-site-builder"
    },
    {
      "id": "sb-editor-sidebar",
      "status": "done",
      "target_file": "services/site-builder-editor-sidebar.js",
      "task": "Render the LEFT pane of the editor workspace: the services/upsell sidebar. Export renderSidebar({ services, clientId, baseUrl }) that returns an HTML STRING FRAGMENT (not a full document) for a vertical list built from services[] (each { id, name, blurb, price, cadence, recommended }). Services flagged recommended show a small 'Recommended' badge and sort first. Clicking a service toggles a 'selected' highlight, adds/removes it from a running 'Your plan' summary at the bottom of the sidebar, and fires a best-effort beacon (navigator.sendBeacon, fall back to fetch with keepalive) to `${baseUrl}/api/v1/sites/select-service?id=<clientId>&service=<id>`. Include a small inline <script> that wires the click behavior via event delegation on the fragment's root element.",
      "spec": "Single self-contained ESM module, ZERO import-time side effects, no network at import, no DB, no project imports. Named export renderSidebar + default export. Returns a STRING (an HTML fragment, NOT a full <html> document). Define a local htmlEscape() and escape name/blurb/price/cadence before interpolation. Guard services as possibly undefined -> default []. The inline script must be wrapped in try/catch and never throw if elements are missing. Deterministic given the same input ordering. @ssot docs/products/site-builder/PRODUCT_HOME.md.",
      "depends_on": [],
      "founder_gated": false,
      "attempts": 3,
      "max_output_tokens": 7168,
      "last_attempt_at": "2026-07-07T01:24:08.815Z",
      "last_error": null,
      "proof": "Built by Cursor agent session 2026-07-07; services/site-builder-editor-sidebar.js committed in repo.",
      "blueprint_id": "PRODUCT-SITE-BUILDER-BUILD-QUEUE-TWIN-V1",
      "blueprint_step_id": "sb-editor-sidebar",
      "mission_id": "PRODUCT-site-builder"
    },
    {
      "id": "sb-editor-canvas",
      "status": "done",
      "target_file": "services/site-builder-editor-canvas.js",
      "task": "Render the CENTER pane of the editor workspace: the live site canvas. Export renderCanvas({ siteFile, variants, palettes, clientId, editToken, baseUrl }) returning an HTML STRING FRAGMENT containing: (1) a top toolbar with template-switcher chips built from variants[] (each {id,name,file}) that set the iframe src, palette swatches from palettes[] (each {name,primary,accent}) that POST a recolor instruction to `${baseUrl}/api/v1/sites/edit`, a mobile/desktop device toggle that sets the iframe width, and a Save button that POSTs the iframe document's serialized outerHTML to `${baseUrl}/api/v1/sites/save-edits` as { clientId, file, html, token }; (2) a same-origin <iframe> loading siteFile; (3) an inline <script> giving click-to-edit (clicking a text node in the iframe makes it contentEditable) and per-top-level-section move-up / move-down / hide controls operating only on iframe.contentDocument top-level children.",
      "spec": "Single self-contained ESM module, ZERO import-time side effects, no project imports. Named export renderCanvas + default export. Returns a STRING (HTML fragment, NOT a full document). Define local htmlEscape(); escape siteFile and all variant/palette text. Every fetch includes the editToken (from a JS const) and on non-2xx shows an inline error — never a silent/blank failure. All iframe DOM access wrapped in try/catch guarding cross-origin/null contentDocument; never throw. Guard variants/palettes as possibly undefined -> []. @ssot docs/products/site-builder/PRODUCT_HOME.md.",
      "depends_on": [],
      "founder_gated": false,
      "attempts": 3,
      "max_output_tokens": 12288,
      "last_attempt_at": "2026-07-07T01:43:52.822Z",
      "last_error": null,
      "proof": "Built by Cursor agent session 2026-07-07; services/site-builder-editor-canvas.js committed in repo.",
      "blueprint_id": "PRODUCT-SITE-BUILDER-BUILD-QUEUE-TWIN-V1",
      "blueprint_step_id": "sb-editor-canvas",
      "mission_id": "PRODUCT-site-builder"
    },
    {
      "id": "sb-editor-chat",
      "status": "done",
      "target_file": "services/site-builder-editor-chat.js",
      "task": "Render the RIGHT pane of the editor workspace: the AI chat + voice panel. Export renderChatPanel({ clientId, editToken, baseUrl }) returning an HTML STRING FRAGMENT with a scrollable message list, a text input, a Send button, and a microphone button. Sending (typed or spoken) appends the user's message to the list and POSTs { clientId, file: null, instruction, token } to `${baseUrl}/api/v1/sites/edit`; on success it appends a confirmation and dispatches a window CustomEvent 'sb-editor:reload' so the canvas can reload its iframe. Include a short helper hint line telling the client they can type or speak what they want changed.",
      "spec": "Single self-contained ESM module, ZERO import-time side effects, no project imports. Named export renderChatPanel + default export. Returns a STRING (HTML fragment). Microphone uses window.webkitSpeechRecognition||window.SpeechRecognition; if unavailable, hide the mic button and show a tooltip 'Voice not supported in this browser' — never throw. Every fetch includes editToken; non-2xx shows an inline error bubble, never blank. Inline script wrapped in try/catch. Define local htmlEscape(). @ssot docs/products/site-builder/PRODUCT_HOME.md.",
      "depends_on": [],
      "founder_gated": false,
      "attempts": 3,
      "max_output_tokens": 8192,
      "last_attempt_at": "2026-07-07T02:04:18.424Z",
      "last_error": null,
      "proof": "Built by Cursor agent session 2026-07-07; services/site-builder-editor-chat.js committed in repo.",
      "blueprint_id": "PRODUCT-SITE-BUILDER-BUILD-QUEUE-TWIN-V1",
      "blueprint_step_id": "sb-editor-chat",
      "mission_id": "PRODUCT-site-builder"
    },
    {
      "id": "sb-editor-strategy",
      "status": "done",
      "target_file": "services/site-builder-editor-strategy.js",
      "task": "Render the competitor STRATEGY view for the editor. Export renderStrategyPanel({ strategy }) returning an HTML STRING FRAGMENT that shows strategy.synopsis (a plain-English paragraph) followed by strategy.competitors[] (each { name, category, score, strengths, weaknesses }) rendered as 1–10 scorecards: the numeric score (0–10) as a labeled bar, category tag, and short strengths/weaknesses lists. Group scorecards by category so midwifery and wellness competitors read as separate lanes. If strategy or competitors is missing/empty, render a friendly 'Competitor analysis will appear here once your benchmark runs' placeholder.",
      "spec": "Single self-contained ESM module, ZERO import-time side effects, no network, no DB, no project imports. Named export renderStrategyPanel + default export. Returns a STRING (HTML fragment). Define local htmlEscape() and escape ALL strategy text (synopsis, names, strengths, weaknesses) before interpolation. Clamp score into 0–10 for the bar width. Guard every field as possibly undefined. Never fabricate competitors or scores — only render what is provided. Deterministic output. @ssot docs/products/site-builder/PRODUCT_HOME.md.",
      "depends_on": [],
      "founder_gated": false,
      "attempts": 3,
      "max_output_tokens": 7168,
      "last_attempt_at": "2026-07-07T02:19:00.085Z",
      "last_error": null,
      "proof": "Built by Cursor agent session 2026-07-07; services/site-builder-editor-strategy.js committed in repo.",
      "blueprint_id": "PRODUCT-SITE-BUILDER-BUILD-QUEUE-TWIN-V1",
      "blueprint_step_id": "sb-editor-strategy",
      "mission_id": "PRODUCT-site-builder"
    },
    {
      "id": "sb-editor-shell",
      "status": "done",
      "target_file": "services/site-builder-editor.js",
      "task": "Compose the full three-pane editor workspace document from the already-built pane modules. Import renderSidebar from './site-builder-editor-sidebar.js', renderCanvas from './site-builder-editor-canvas.js', renderChatPanel from './site-builder-editor-chat.js', and renderStrategyPanel from './site-builder-editor-strategy.js'. Export renderEditorShell({ businessName, clientId, siteFile, variants, palettes, editToken, baseUrl, strategy, services }) that returns ONE complete HTML document: a top bar showing the business name, then a THREE-COLUMN layout — LEFT = renderSidebar({ services, clientId, baseUrl }), CENTER = renderCanvas({ siteFile, variants, palettes, clientId, editToken, baseUrl }), RIGHT = a right column containing renderChatPanel({ clientId, editToken, baseUrl }) above a collapsible 'Strategy' section holding renderStrategyPanel({ strategy }). Add a small inline <script> that listens for the 'sb-editor:reload' window event and reloads the center iframe. This module is ONLY composition + document scaffold — it must not re-implement pane internals.",
      "spec": "Single self-contained ESM module. It MAY import the four sibling pane modules by relative path (they are committed before this step via depends_on). Named export renderEditorShell + default export. Returns a full HTML document using Tailwind CDN (https://cdn.tailwindcss.com) and Alpine.js CDN (https://unpkg.com/alpinejs@3.x.x/dist/cdn.min.js, defer). Define a local htmlEscape() and escape businessName before interpolation. Guard every destructured field (default services/variants/palettes/strategy sensibly). No DB, no network at import time. Keep this file small — delegate all pane markup to the imported render functions. @ssot docs/products/site-builder/PRODUCT_HOME.md.",
      "depends_on": [
        "sb-editor-sidebar",
        "sb-editor-canvas",
        "sb-editor-chat",
        "sb-editor-strategy"
      ],
      "founder_gated": false,
      "attempts": 1,
      "max_output_tokens": 8192,
      "proof": "Built by Cursor agent session 2026-07-07; services/site-builder-editor.js",
      "blueprint_id": "PRODUCT-SITE-BUILDER-BUILD-QUEUE-TWIN-V1",
      "blueprint_step_id": "sb-editor-shell",
      "mission_id": "PRODUCT-site-builder"
    },
    {
      "id": "sb-editor-endpoints",
      "status": "done",
      "target_file": "routes/site-builder-editor-routes.js",
      "task": "New Express route module exporting createSiteBuilderEditorRoutes(app, { callCouncilMember, baseUrl }) that mounts three POST endpoints under /api/v1/sites: `/edit` (AI edit), `/save-edits` (manual save), and `/revert` (restore the most recent .bak). All three authenticate with a PER-PREVIEW token (NOT the global command key): read public/previews/<clientId>/meta.json and require body.token === meta.editToken, else respond 403. `/edit` reads the target file (body.file, relative, under the client's preview dir), calls callCouncilMember with a STRONG paid model, passing the current HTML + the user's instruction + strict anti-fabrication TRUTH rules, asking it to return the ENTIRE modified HTML document; validate the response contains '<html'; write a timestamped .bak of the old file then overwrite; respond { ok:true }. `/save-edits` validates body.html contains '<html', writes a .bak then overwrites body.file. `/revert` restores the newest .bak for body.file.",
      "spec": "Single self-contained ESM file. Named export createSiteBuilderEditorRoutes + default. PATH SAFETY (hard, fail-closed): resolve previewsRoot = path.resolve(process.cwd(),'public/previews') ONCE; for every file access resolve the candidate and require resolved.startsWith(previewsRoot + path.sep) AND clientId matches /^[\\w-]+$/ AND file has no '..' — otherwise 403. Never read/write outside previewsRoot. Token check fails closed when meta.json is missing, unreadable, or has no editToken. No database. Use express.json() body parsing already present. Wrap AI/file errors and respond JSON { ok:false, error } with appropriate status; never leak absolute paths in errors. TRUTH rules passed to the model must forbid inventing prices, stats, ratings, review counts, and named testimonials. @ssot docs/products/site-builder/PRODUCT_HOME.md.",
      "depends_on": [
        "sb-editor-shell"
      ],
      "founder_gated": false,
      "attempts": 1,
      "proof": "Built by Cursor agent session 2026-07-07; routes/site-builder-editor-routes.js",
      "blueprint_id": "PRODUCT-SITE-BUILDER-BUILD-QUEUE-TWIN-V1",
      "blueprint_step_id": "sb-editor-endpoints",
      "mission_id": "PRODUCT-site-builder"
    },
    {
      "id": "sb-competitor-categories",
      "status": "done",
      "target_file": "services/site-builder-competitor-categories.js",
      "task": "Create a helper that classifies a wellness business into ALL of its practice categories (a business can be several at once — e.g. midwife AND general wellness) and returns per-category competitor search queries. Export classifyPracticeCategories(businessInfo) -> { categories: [{ id, label, searchQueries[] }] } covering at least 'midwifery' (home birth, midwife, doula, birth support) and 'wellness' (holistic wellness, wellness practitioner) detection from businessInfo.industry/services/keywords, and buildDualCategoryQueries(businessInfo) that always returns queries for BOTH midwifery and wellness when either is detected, so the benchmark compares against both lanes.",
      "spec": "Single self-contained ESM module, pure functions, no network/DB/imports. Named exports classifyPracticeCategories and buildDualCategoryQueries + default export object. Case-insensitive keyword matching over arrays that may be undefined (guard everything). Deterministic output. Never throw on missing fields. @ssot docs/products/site-builder/PRODUCT_HOME.md.",
      "depends_on": [],
      "founder_gated": false,
      "attempts": 1,
      "commit_sha": "6cce97ef3",
      "proof": "Built autonomously by the never-stop loop once the prod GitHub token was restored; committed as services/site-builder-competitor-categories.js. node --check passes.",
      "blueprint_id": "PRODUCT-SITE-BUILDER-BUILD-QUEUE-TWIN-V1",
      "blueprint_step_id": "sb-competitor-categories",
      "mission_id": "PRODUCT-site-builder"
    },
    {
      "id": "sb-social-discovery",
      "status": "done",
      "target_file": "services/site-builder-social-discovery.js",
      "task": "Create a social-media discovery helper that finds a business's social profiles — especially YouTube — even when not linked on their website. Export async discoverSocials(businessInfo, { fetchImpl }) that (1) collects any social URLs already present in businessInfo, and (2) builds candidate handles from the business name and, when a fetchImpl is provided, verifies likely YouTube (youtube.com/@handle), Instagram, and Facebook URLs by HEAD/GET and returns only reachable ones. Also export extractYouTubeChannelId(html) to pull a channelId from a YouTube page's HTML. Returns { youtube: {url,channelId}|null, instagram, facebook, tiktok, others[] }.",
      "spec": "Single self-contained ESM module. Named exports discoverSocials, extractYouTubeChannelId + default. NETWORK IS OPTIONAL AND INJECTED: never import a network library; use the provided fetchImpl (default to globalThis.fetch) and if it is unavailable, return only the socials already known from businessInfo (fail-open to no-op, never throw). Wrap every fetch in try/catch with a short timeout via AbortController. Do not fabricate a profile — only return URLs actually confirmed reachable or already provided. Guard all businessInfo fields. @ssot docs/products/site-builder/PRODUCT_HOME.md.",
      "depends_on": [],
      "founder_gated": false,
      "attempts": 1,
      "commit_sha": "108ab3ccbfb5f3aed44b1c763653151a3b80aee5",
      "last_attempt_at": "2026-07-07T21:48:07.356Z",
      "completed_at": "2026-07-07T21:48:09.306Z",
      "blueprint_id": "PRODUCT-SITE-BUILDER-BUILD-QUEUE-TWIN-V1",
      "blueprint_step_id": "sb-social-discovery",
      "mission_id": "PRODUCT-site-builder"
    },
    {
      "id": "sb-service-catalog",
      "status": "done",
      "target_file": "services/site-builder-service-catalog.js",
      "task": "Create the à-la-carte add-on SERVICE CATALOG that powers the editor's left sidebar. Export SERVICE_CATALOG (array of { id, name, blurb, price, cadence }) covering: logo & brand kit, corporate/brand package, ClickFunnels build + ad placement/management, managed Google Business Profile + local SEO, social-media management, and an SEO/content care plan — with honest, GOAL-based (never GUARANTEE) copy. For the Google offering use exactly this framing: 'managed Google Business Profile + local SEO with the GOAL of ranking on the first page of results for your category' — a goal/minimum-expectation, NEVER a guarantee and NEVER 'guaranteed #1'. Each service may carry an optional `results` proof object ({ headline, sampleSize, onFirstPage, atNumberOne }) — real, MEASURED client outcomes only (e.g. '11 of 15 managed businesses rank on page one, 9 at #1'); export formatResultsProof(results) that returns a short honest string ONLY when results.sampleSize>0, and returns null (hide the block) otherwise — NEVER fabricate outcome numbers. Export recommendServices(strategy) that, given the competitor strategy object ({ competitors:[{doesPoorly/weaknesses}], gaps }), returns the SAME catalog with a boolean `recommended` flag set on the services that address the biggest competitor gaps (e.g. flag ad-management/funnel when competitors lack ads, flag video/social when competitors lack video). Prices come from the product revenue model, not invented.",
      "spec": "Single self-contained ESM module, pure data + pure functions, no network/DB/imports/side effects. Named exports SERVICE_CATALOG, recommendServices, formatResultsProof + default. recommendServices must never throw on missing/partial strategy (guard everything, default to no recommendations). Copy must comply with the product's TRUTH non-negotiables: no guaranteed rankings, no fabricated outcomes. Deterministic output. @ssot docs/products/site-builder/PRODUCT_HOME.md.",
      "depends_on": [],
      "founder_gated": false,
      "attempts": 1,
      "proof": "Built by Cursor agent session 2026-07-07; services/site-builder-service-catalog.js prices wired from config/site-builder-pricing.js",
      "blueprint_id": "PRODUCT-SITE-BUILDER-BUILD-QUEUE-TWIN-V1",
      "blueprint_step_id": "sb-service-catalog",
      "mission_id": "PRODUCT-site-builder"
    },
    {
      "id": "sb-editor-onboarding-copy",
      "status": "done",
      "target_file": "services/site-builder-editor-onboarding.js",
      "task": "Create the guided-onboarding copy module the editor panes consume so a non-technical client is never staring at a bare editor. This directly answers the SENTRY Layer B UX critique of the live editor (verdict 'rough'): the chat wasn't self-explanatory, palettes weren't labeled in plain language, and there was no first-step guidance. Export CHAT_WELCOME (a short first message the AI chat panel shows on open, e.g. 'Hi! Tell me what to change — like \"make the header bigger\", \"use calmer colors\", or \"add a booking button\" — and I'll update your site.'), PALETTE_LABELS (a map from palette name/vibe to a plain-language label like Calm, Bright, Elegant, Clinical, Warm), labelPalette(palette) -> friendly string, and EDITOR_FIRST_STEPS (an ordered array of 3-4 short next-step hints, e.g. 'Pick a look up top', 'Click any text to edit it', 'Ask the assistant on the right for changes', 'Hit Publish when you love it').",
      "spec": "Single self-contained ESM module, pure data + pure functions, ZERO import-time side effects, no network/DB/project imports. Named exports CHAT_WELCOME, PALETTE_LABELS, labelPalette, EDITOR_FIRST_STEPS + default export object. labelPalette must guard undefined/unknown input and fall back to the palette's own name (never throw). Copy is plain-language, encouraging, non-technical, and must not fabricate features that don't exist in the editor. Deterministic output. @ssot docs/products/site-builder/PRODUCT_HOME.md.",
      "depends_on": [],
      "founder_gated": false,
      "attempts": 1,
      "source": "SENTRY Layer B pre-alpha UX critique (live run on onlinewellroundedmama.com, prev_1783488127928_tg15): friction_points = limited templates/palettes, unclear chat purpose, unguided editor.",
      "commit_sha": "94c89f90e1ef1c20b677ba53f9c5cf8f1b8e1af3",
      "last_attempt_at": "2026-07-08T06:07:09.460Z",
      "last_error": "verify_exit_1",
      "revive_count": 1,
      "completed_at": "2026-07-08T06:07:18.318Z",
      "blueprint_id": "PRODUCT-SITE-BUILDER-BUILD-QUEUE-TWIN-V1",
      "blueprint_step_id": "sb-editor-onboarding-copy",
      "mission_id": "PRODUCT-site-builder"
    },
    {
      "id": "step-1",
      "status": "done",
      "target_file": "db/migrations/20240801_site_builder_templates.sql",
      "task": "Create starter templates table",
      "spec": "CREATE TABLE IF NOT EXISTS site_builder_templates with columns id UUID PK default gen_random_uuid(), name TEXT NOT NULL, thumbnail_url TEXT, config JSONB NOT NULL, sort_order INT DEFAULT 0, created_at TIMESTAMPTZ DEFAULT now(), updated_at TIMESTAMPTZ DEFAULT now(). Seed 5 rows for distinct starter templates (e.g. 'Clean Business','Bold Agency','Minimal Portfolio','Warm Local','Tech Startup') using INSERT INTO ... ON CONFLICT DO NOTHING so re-runs are safe. Satisfies ux_friction_1 data layer.",
      "depends_on": [],
      "founder_gated": false,
      "attempts": 1,
      "commit_sha": "5b1c400053580235a6db1e8d97cc2f5feb1f8954",
      "last_attempt_at": "2026-07-10T21:08:39.413Z",
      "completed_at": "2026-07-10T21:08:56.342Z",
      "blueprint_id": "PRODUCT-SITE-BUILDER-BUILD-QUEUE-TWIN-V1",
      "blueprint_step_id": "step-1",
      "mission_id": "PRODUCT-site-builder"
    },
    {
      "id": "step-2",
      "status": "done",
      "target_file": "services/site-builder-templates.js",
      "task": "Implement template listing and selection service",
      "spec": "Export async functions listTemplates(db) returning all rows from site_builder_templates ordered by sort_order, and getTemplate(db, id) returning one row. No browser globals. No AI calls. Satisfies ux_friction_1 service layer. Import only 'pg' or the injected db dep.",
      "depends_on": [
        "step-1"
      ],
      "founder_gated": false,
      "attempts": 1,
      "commit_sha": "aca57d28d31e28bbd979642a8e8578623159ad12",
      "last_attempt_at": "2026-07-10T21:12:05.319Z",
      "completed_at": "2026-07-10T21:12:15.674Z",
      "blueprint_id": "PRODUCT-SITE-BUILDER-BUILD-QUEUE-TWIN-V1",
      "blueprint_step_id": "step-2",
      "mission_id": "PRODUCT-site-builder"
    },
    {
      "id": "step-3",
      "status": "done",
      "target_file": "services/site-builder-save-confirmation.js",
      "task": "Implement save-confirmation timestamp service",
      "spec": "Export function buildSaveConfirmation() that returns a plain JS object {savedAt: new Date().toISOString(), label: 'Saved', icon: 'checkmark'}. Export function formatSaveConfirmationHTML(obj) that returns a safe HTML string '<span class=\"sb-save-confirm\">✓ Saved at HH:MM:SS</span>' computed from obj.savedAt. No browser globals, no imports beyond node builtins. Satisfies ux_friction_4 service layer.",
      "depends_on": [],
      "founder_gated": false,
      "attempts": 1,
      "commit_sha": "b6aaf4b796693f4a8ebd75c81fc109b5da75cc8b",
      "last_attempt_at": "2026-07-10T21:14:50.477Z",
      "completed_at": "2026-07-10T21:14:59.670Z",
      "blueprint_id": "PRODUCT-SITE-BUILDER-BUILD-QUEUE-TWIN-V1",
      "blueprint_step_id": "step-3",
      "mission_id": "PRODUCT-site-builder"
    },
    {
      "id": "step-4",
      "status": "done",
      "target_file": "routes/site-builder-templates-routes.js",
      "task": "Register REST routes for template listing and selection",
      "spec": "Export registerSiteBuilderTemplatesRoutes(app, deps) where deps includes db. Mount GET /api/site-builder/templates → calls listTemplates(deps.db) and returns JSON array. Mount GET /api/site-builder/templates/:id → calls getTemplate(deps.db, req.params.id) and returns JSON object or 404. No mutations. No browser globals. Must be importable without side-effects. Satisfies ux_friction_1 API layer.",
      "depends_on": [
        "step-2"
      ],
      "founder_gated": false,
      "attempts": 1,
      "commit_sha": "aab32f8c41e4cf015c650bd4de1ebd06cbfbb7a0",
      "proof": "Module already exported registerSiteBuilderTemplatesRoutes + list/get; GAP-FILL closed step-5 mount so tip can serve GET /api/site-builder/templates.",
      "completed_at": "2026-07-11T08:55:00.000Z",
      "last_attempt_at": "2026-07-11T08:55:00.000Z",
      "last_error": null,
      "blueprint_id": "PRODUCT-SITE-BUILDER-BUILD-QUEUE-TWIN-V1",
      "blueprint_step_id": "step-4",
      "mission_id": "PRODUCT-site-builder"
    },
    {
      "id": "step-5",
      "status": "done",
      "target_file": "config/auto-registered-product-modules.json",
      "task": "Register template routes module in auto-registration config",
      "spec": "Add entry for site-builder-templates-routes to the JSON array so the boot system loads registerSiteBuilderTemplatesRoutes automatically. Preserve all existing entries. No other changes.",
      "depends_on": [
        "step-4"
      ],
      "founder_gated": false,
      "attempts": 1,
      "proof": "GAP-FILL 2026-07-11: added routes/site-builder-templates-routes.js → registerSiteBuilderTemplatesRoutes to auto-registered-product-modules.json (first-dollar unblock).",
      "completed_at": "2026-07-11T08:55:00.000Z",
      "blueprint_id": "PRODUCT-SITE-BUILDER-BUILD-QUEUE-TWIN-V1",
      "blueprint_step_id": "step-5",
      "mission_id": "PRODUCT-site-builder"
    },
    {
      "id": "step-6",
      "status": "done",
      "target_file": "public/overlay/site-builder-template-picker.html",
      "task": "Build template picker UI overlay showing 3–5 starter templates",
      "spec": "Self-contained HTML fragment rendered as an overlay panel. Fetches GET /api/site-builder/templates via client-side fetch and renders a card grid (thumbnail, name, 'Use this template' CTA button) for each template. Cards use CSS grid, minimum 3 columns on desktop. Selecting a card posts the chosen template id back to the editor state and closes the overlay. Includes clear heading 'Choose a starting design' and subtext 'Pick the look that fits your business — you can change everything later.' Satisfies ux_friction_1 UI. All JS inside inline <script> tags. No external CDN dependencies.",
      "depends_on": [
        "step-4"
      ],
      "founder_gated": true,
      "attempts": 0,
      "commit_sha": "948f412500d3e58c0194226e00eac2058347c41d",
      "blueprint_id": "PRODUCT-SITE-BUILDER-BUILD-QUEUE-TWIN-V1",
      "blueprint_step_id": "step-6",
      "mission_id": "PRODUCT-site-builder"
    },
    {
      "id": "step-7",
      "status": "done",
      "target_file": "public/overlay/site-builder-editor-onboarding-prompt.html",
      "task": "Build first-step onboarding prompt UI for the editor",
      "spec": "Self-contained HTML fragment shown at editor launch (dismissible). Contains a styled banner with the text 'Start here: choose a template, then edit your text.' and two CTA buttons: 'Pick a template' (opens the template picker overlay) and 'I'll explore on my own' (dismisses the banner and sets localStorage key sb_onboarding_dismissed=1 so it does not reappear). No backend calls required. All JS inline. Satisfies ux_friction_2.",
      "depends_on": [
        "step-6"
      ],
      "founder_gated": true,
      "attempts": 0,
      "shipped_via": "governed_ship_queue",
      "shipped_at": "2026-07-17T20:04:43.913Z",
      "commit_sha": "6a6b83bf2d598b9b1709e8634b35d5f84b71c9f8",
      "blueprint_id": "PRODUCT-SITE-BUILDER-BUILD-QUEUE-TWIN-V1",
      "blueprint_step_id": "step-7",
      "mission_id": "PRODUCT-site-builder"
    },
    {
      "id": "step-8",
      "status": "done",
      "target_file": "public/overlay/site-builder-chat-prompt.html",
      "task": "Build chat prompt UI with example questions",
      "spec": "Self-contained HTML fragment that enhances the chat panel. Renders a 'Not sure what to ask?' section above the chat input with 3 clickable example chips: 'Change my colors', 'Rewrite my homepage headline', 'Add a contact section'. Clicking a chip pre-fills the chat input with that text. Includes a visible heading 'Ask the AI assistant' and brief subtext 'Type a request or tap an example below.' Satisfies ux_friction_3. All JS inline, no external deps.",
      "depends_on": [],
      "founder_gated": true,
      "attempts": 0,
      "shipped_via": "governed_ship_queue",
      "shipped_at": "2026-07-17T20:05:48.867Z",
      "commit_sha": "652f0c09d1c6abdd2c959bbb7e15077add2fb347",
      "blueprint_id": "PRODUCT-SITE-BUILDER-BUILD-QUEUE-TWIN-V1",
      "blueprint_step_id": "step-8",
      "mission_id": "PRODUCT-site-builder"
    },
    {
      "id": "step-9",
      "status": "done",
      "target_file": "public/overlay/site-builder-save-confirmation.html",
      "task": "Build save confirmation UI widget with timestamp and checkmark",
      "spec": "Self-contained HTML fragment that mounts a save-status bar at the top of the editor. Listens for a custom DOM event 'sb:saved' dispatched by the editor on successful save. On event receipt renders '✓ Saved at HH:MM:SS' where time is derived from event.detail.savedAt, displayed for 4 seconds then fades out with CSS transition. Before any save the bar reads 'Unsaved changes' in muted style. Satisfies ux_friction_4 UI. All JS inline.",
      "depends_on": [
        "step-3"
      ],
      "founder_gated": true,
      "attempts": 0,
      "shipped_via": "governed_ship_queue",
      "shipped_at": "2026-07-17T20:09:27.543Z",
      "commit_sha": "e054c4d1f5e510a08cf6d54fb6031437a7dc6c0d",
      "blueprint_id": "PRODUCT-SITE-BUILDER-BUILD-QUEUE-TWIN-V1",
      "blueprint_step_id": "step-9",
      "mission_id": "PRODUCT-site-builder"
    },
    {
      "id": "step-10",
      "status": "done",
      "target_file": "public/overlay/site-builder-device-toggle.html",
      "task": "Build plainly-labelled Desktop/Tablet/Mobile device toggle UI",
      "spec": "Self-contained HTML fragment replacing or augmenting any unlabelled icon-only device toggle. Renders three clearly labelled buttons: 'Desktop', 'Tablet', 'Mobile'. Active button is highlighted. Clicking each dispatches custom DOM event 'sb:deviceChange' with detail {mode:'desktop'|'tablet'|'mobile'} so the canvas can react. Buttons include an accessible aria-label and a small intuitive icon (unicode or inline SVG) alongside the plain text label. Satisfies ux_friction_5. All JS inline.",
      "depends_on": [],
      "founder_gated": true,
      "attempts": 0,
      "shipped_via": "governed_ship_queue",
      "shipped_at": "2026-07-17T20:17:04.918Z",
      "commit_sha": "aba09128e4a4ba2da57e7d1987969e23d6d054e2",
      "blueprint_id": "PRODUCT-SITE-BUILDER-BUILD-QUEUE-TWIN-V1",
      "blueprint_step_id": "step-10",
      "mission_id": "PRODUCT-site-builder"
    },
    {
      "id": "step-01",
      "status": "done",
      "target_file": "db/migrations/20240915_site_builder_ab_subject.sql",
      "task": "Create AB subject-line test tables",
      "spec": "CREATE TABLE IF NOT EXISTS site_builder_ab_subject_tests with columns: id uuid PK default gen_random_uuid(), site_id uuid NOT NULL, variant_a text NOT NULL, variant_b text NOT NULL, winner text, sends_a int default 0, sends_b int default 0, opens_a int default 0, opens_b int default 0, status text default 'active', created_at timestamptz default now(), updated_at timestamptz default now(). CREATE TABLE IF NOT EXISTS site_builder_ab_subject_events with id uuid PK gen_random_uuid(), test_id uuid NOT NULL REFERENCES site_builder_ab_subject_tests(id), variant text NOT NULL, event_type text NOT NULL, occurred_at timestamptz default now(). Both CREATE TABLE IF NOT EXISTS only, no drops or alters.",
      "depends_on": [],
      "founder_gated": false,
      "attempts": 1,
      "commit_sha": "2c3dea34d82f5f0482c4189e9825b68b0583103b",
      "last_attempt_at": "2026-07-11T09:05:05.742Z",
      "completed_at": "2026-07-11T09:05:19.931Z",
      "blueprint_id": "PRODUCT-SITE-BUILDER-BUILD-QUEUE-TWIN-V1",
      "blueprint_step_id": "step-01",
      "mission_id": "PRODUCT-site-builder"
    },
    {
      "id": "step-02",
      "status": "done",
      "target_file": "services/site-builder-ab-subject-testing.js",
      "task": "Implement A/B subject-line testing service",
      "spec": "Export createTest(db,siteId,variantA,variantB), recordEvent(db,testId,variant,eventType), pickVariant(db,testId) (returns 'a' or 'b' via simple open-rate comparison, defaults to 'a' when tied or insufficient data), concludeTest(db,testId) (sets winner and status='concluded' based on open rate). No AI, no browser globals, uses only pg or the db client passed as argument. All SQL uses parameterised queries. Module must work standalone after step-01 migration is applied.",
      "depends_on": [
        "step-01"
      ],
      "founder_gated": false,
      "attempts": 1,
      "commit_sha": "62ea2eed60790cee8833758c6dc12261a890390a",
      "last_attempt_at": "2026-07-11T09:07:04.555Z",
      "completed_at": "2026-07-11T09:07:16.932Z",
      "blueprint_id": "PRODUCT-SITE-BUILDER-BUILD-QUEUE-TWIN-V1",
      "blueprint_step_id": "step-02",
      "mission_id": "PRODUCT-site-builder"
    },
    {
      "id": "step-03",
      "status": "done",
      "target_file": "routes/site-builder-ab-subject-routes.js",
      "task": "Mount REST routes for A/B subject-line testing",
      "spec": "Export registerAbSubjectRoutes(app,deps) where deps provides db. Mount POST /api/site-builder/ab-subject/tests (create), POST /api/site-builder/ab-subject/tests/:testId/events (record), GET /api/site-builder/ab-subject/tests/:testId (fetch with stats), POST /api/site-builder/ab-subject/tests/:testId/conclude. All routes validate required fields and return JSON. Import service from step-02 only. Do NOT touch server.js.",
      "depends_on": [
        "step-02"
      ],
      "founder_gated": false,
      "attempts": 1,
      "proof": "GAP-FILL 2026-07-11: routes file already [system-build]; auto-registered registerSiteBuilderAbSubjectRoutes so tip mounts LIVE.",
      "completed_at": "2026-07-11T12:55:00.000Z",
      "blueprint_id": "PRODUCT-SITE-BUILDER-BUILD-QUEUE-TWIN-V1",
      "blueprint_step_id": "step-03",
      "mission_id": "PRODUCT-site-builder"
    },
    {
      "id": "step-05",
      "status": "done",
      "target_file": "scripts/site-builder-preview-expiry-sweep.mjs",
      "task": "Implement preview-expiry sweep script",
      "spec": "ESM script (.mjs). Connects to the database using DATABASE_URL from process.env. Queries for site_builder_previews rows where expires_at < now() and deleted_at IS NULL. Marks them deleted by setting deleted_at=now(). Logs count of swept rows. Accepts optional --dry-run flag that logs but does not write. Imports only node builtins (url, process) and pg (already installed). No browser globals. Exits with code 0 on success, 1 on error. Does NOT conflict with or duplicate services/site-builder-save-confirmation.js or scripts/site-builder-preview-expiry-cron.mjs; the cron file calls this sweep logic.",
      "depends_on": [],
      "founder_gated": false,
      "attempts": 1,
      "proof": "Already satisfied by sb-preview-expiry-sweep → scripts/site-builder-preview-expiry-cron.mjs (done). Planner duplicate closed without second file.",
      "completed_at": "2026-07-11T12:55:00.000Z",
      "blueprint_id": "PRODUCT-SITE-BUILDER-BUILD-QUEUE-TWIN-V1",
      "blueprint_step_id": "step-05",
      "mission_id": "PRODUCT-site-builder"
    },
    {
      "id": "step-06",
      "status": "done",
      "target_file": "services/site-builder-template-options.js",
      "task": "Expand template options service to address ux_friction_1",
      "spec": "Export getTemplateOptions() returning an array of at least 5 distinct template descriptors each with {id, name, description, thumbnailUrl, tags:[]}. Tags must include style keywords (e.g. 'minimal','bold','classic','modern','playful') so the UI can filter. Export getTemplateById(id). No DB required — data is module-level const. No browser globals. This file is new and does not alter the existing services/site-builder-templates.js behaviour.",
      "depends_on": [],
      "founder_gated": true,
      "attempts": 0,
      "expected_exports": [
        "getTemplateOptions",
        "getTemplateById"
      ],
      "behavior_assertions": [
        {
          "type": "exports_smoke",
          "path": "services/site-builder-template-options.js",
          "exports": [
            "getTemplateOptions",
            "getTemplateById"
          ],
          "assertion_id": "expected_exports:services/site-builder-template-options.js"
        }
      ],
      "shipped_via": "governed_ship_queue",
      "shipped_at": "2026-07-17T20:18:18.035Z",
      "commit_sha": "855533e53f1ec5870fe2dc540905afc38d410b6e",
      "blueprint_id": "PRODUCT-SITE-BUILDER-BUILD-QUEUE-TWIN-V1",
      "blueprint_step_id": "step-06",
      "mission_id": "PRODUCT-site-builder"
    },
    {
      "id": "step-07",
      "status": "done",
      "target_file": "services/site-builder-color-palettes.js",
      "task": "Expand color palette options to address ux_friction_2",
      "spec": "Export getColorPalettes() returning an array of at least 8 palettes each with {id, label, description, primary, secondary, accent, background, text}. Labels are human-readable brand-style names (e.g. 'Ocean Calm', 'Bold Contrast'). Export getPaletteById(id). No DB, no browser globals, module-level const data only.",
      "depends_on": [],
      "founder_gated": true,
      "attempts": 0,
      "expected_exports": [
        "getColorPalettes",
        "getPaletteById"
      ],
      "behavior_assertions": [
        {
          "type": "exports_smoke",
          "path": "services/site-builder-color-palettes.js",
          "exports": [
            "getColorPalettes",
            "getPaletteById"
          ],
          "assertion_id": "expected_exports:services/site-builder-color-palettes.js"
        }
      ],
      "shipped_via": "governed_ship_queue",
      "shipped_at": "2026-07-17T20:24:36.655Z",
      "commit_sha": "b819a9beb7f4ab7448cc4ab51cd75f86e3f0f769",
      "blueprint_id": "PRODUCT-SITE-BUILDER-BUILD-QUEUE-TWIN-V1",
      "blueprint_step_id": "step-07",
      "mission_id": "PRODUCT-site-builder"
    },
    {
      "id": "step-08",
      "status": "skipped",
      "target_file": "routes/site-builder-customization-routes.js",
      "task": "Mount customization UI routes serving template and palette choices",
      "spec": "Export registerCustomizationRoutes(app,deps). Mount GET /api/site-builder/customization/templates (returns JSON array from step-06), GET /api/site-builder/customization/templates/:id, GET /api/site-builder/customization/palettes (returns JSON from step-07), GET /api/site-builder/customization/palettes/:id. Mount GET /site-builder/customization which returns a self-contained HTML page (inline in the returned string) with: a template picker grid showing all templates with thumbnails and style tags (addresses ux_friction_1); a colour palette picker showing all palettes with swatches and human-readable labels (addresses ux_friction_2); icon-button tooltips/labels for chat, save, and device-toggle controls (addresses ux_friction_2 label helper-text fix); a getting-started prompt banner 'Choose a template, then pick a colour palette' visible until the user makes both selections (addresses ux_friction_3); a device-view toggle that shows a side-by-side before/after preview panel when switching between mobile and desktop (addresses ux_friction_4). All interactive JS lives inside the returned HTML string. No browser globals at module level. Import services from step-06 and step-07 only.",
      "depends_on": [
        "step-06",
        "step-07"
      ],
      "founder_gated": true,
      "attempts": 1,
      "expected_exports": [
        "registerCustomizationRoutes",
        "onRoutes"
      ],
      "behavior_assertions": [
        {
          "type": "exports_smoke",
          "path": "routes/site-builder-customization-routes.js",
          "exports": [
            "registerCustomizationRoutes",
            "onRoutes"
          ],
          "assertion_id": "expected_exports:routes/site-builder-customization-routes.js"
        }
      ],
      "route": {
        "method": "GET",
        "path": "/api/site-builder/customization/templates"
      },
      "last_attempt_at": "2026-07-17T21:45:30.505Z",
      "last_error": "NOT_ON_BLUEPRINT",
      "commit_sha": null,
      "built_sha": null,
      "proof": null,
      "revive_count": 7,
      "revived_at": "2026-07-17T21:45:27.597Z",
      "last_attempt": null,
      "demoted": true,
      "demote_reason": "revive_exhausted:NOT_ON_BLUEPRINT",
      "demoted_at": "2026-07-17T21:51:17.008Z",
      "park_until": null,
      "no_op": null,
      "pre_existing": null,
      "blocker_class": null,
      "claim_level": null,
      "blocker_type": null,
      "blocker_resolution": null,
      "blueprint_id": "PRODUCT-SITE-BUILDER-BUILD-QUEUE-TWIN-V1",
      "blueprint_step_id": "step-08",
      "mission_id": "PRODUCT-site-builder"
    },
    {
      "id": "step-11",
      "status": "done",
      "target_file": "services/site-builder-preview-expiry-ui.js",
      "task": "Add UI-layer helpers for preview-expiry sweep feature",
      "spec": "Export formatExpirySweepReport(swept) where swept is array of {previewId, expiredAt}; returns HTML string summarising count and listing ids. Export getExpirySweepStatusBadge(lastRanAt) returning a string badge like 'Last ran: 5 min ago' or 'Never run'. Depends only on already-built scripts/site-builder-preview-expiry-sweep.mjs conceptually; imports nothing from it (sweep runs as cron). No browser globals.",
      "depends_on": [],
      "founder_gated": true,
      "attempts": 0,
      "expected_exports": [
        "formatExpirySweepReport",
        "getExpirySweepStatusBadge"
      ],
      "behavior_assertions": [
        {
          "type": "exports_smoke",
          "path": "services/site-builder-preview-expiry-ui.js",
          "exports": [
            "formatExpirySweepReport",
            "getExpirySweepStatusBadge"
          ],
          "assertion_id": "expected_exports:services/site-builder-preview-expiry-ui.js"
        }
      ],
      "blueprint_id": "PRODUCT-SITE-BUILDER-BUILD-QUEUE-TWIN-V1",
      "blueprint_step_id": "step-11",
      "mission_id": "PRODUCT-site-builder",
      "shipped_via": "governed_ship_queue",
      "shipped_at": "2026-07-17T21:51:24.371Z",
      "commit_sha": "04ce418418c7469d46b4f10a3752820317cae9c4"
    },
    {
      "id": "step-12",
      "status": "founder_gated",
      "target_file": "public/overlay/site-builder-editor-hints-overlay.html",
      "task": "Build founder-gated editor hints overlay embedding onboarding banner, chat hints, control tooltips, and device preview diff UI",
      "spec": "Self-contained HTML page. On load fetches GET /api/site-builder/ux-hints and renders: (1) onboarding banner at top with step-by-step copy, (2) chat placeholder hint displayed above a mock chat input, (3) tooltip labels for chat/save/deviceToggle controls shown as styled badges, (4) a before/after device preview diff section that lets user paste two HTML snippets and click Compare to POST /api/site-builder/device-preview-diff and display the summary. All controls have visible labels. No server-side code.",
      "depends_on": [
        "step-08"
      ],
      "founder_gated": true,
      "attempts": 0,
      "blueprint_id": "PRODUCT-SITE-BUILDER-BUILD-QUEUE-TWIN-V1",
      "blueprint_step_id": "step-12",
      "mission_id": "PRODUCT-site-builder"
    },
    {
      "id": "step-09",
      "status": "founder_gated",
      "target_file": "routes/site-builder-customization-ui-routes.js",
      "task": "Register customization UI API and HTML routes",
      "spec": "Exports registerCustomizationUiRoutes(app, deps). Mounts GET /site-builder/customization (returns HTML page embedding template/palette pickers populated from getCustomizationOptions); POST /api/site-builder/customization (calls saveCustomizationOptions, returns 200 JSON). HTML is an inline string with client JS that fetches /api/site-builder/colour-palettes and /api/site-builder/templates to populate selectors dynamically. No top-level browser globals. Founder-gated route must check deps.isFounder or equivalent flag before serving. Addresses ux_friction_1 and ux_friction_2.",
      "depends_on": [
        "step-08"
      ],
      "founder_gated": true,
      "attempts": 0,
      "expected_exports": [
        "registerCustomizationUiRoutes",
        "UiRoutes"
      ],
      "behavior_assertions": [
        {
          "type": "exports_smoke",
          "path": "routes/site-builder-customization-ui-routes.js",
          "exports": [
            "registerCustomizationUiRoutes",
            "UiRoutes"
          ],
          "assertion_id": "expected_exports:routes/site-builder-customization-ui-routes.js"
        }
      ],
      "route": {
        "method": "GET",
        "path": "/site-builder/customization"
      },
      "blueprint_id": "PRODUCT-SITE-BUILDER-BUILD-QUEUE-TWIN-V1",
      "blueprint_step_id": "step-09",
      "mission_id": "PRODUCT-site-builder"
    },
    {
      "id": "step-04",
      "status": "done",
      "target_file": "routes/site-builder-color-palettes-routes.js",
      "task": "Register REST routes for color palettes and mount via auto-registration",
      "spec": "Export registerSiteBuilderColorPalettesRoutes(app, deps) where deps includes db. Mount GET /api/site-builder/color-palettes (returns all palettes) and GET /api/site-builder/color-palettes/:id. Import site-builder-color-palettes.js from step-03. After verifying tests pass, add an entry for this module to config/auto-registered-product-modules.json so the framework mounts it automatically -- do NOT edit server.js.",
      "depends_on": [
        "step-03"
      ],
      "founder_gated": false,
      "attempts": 0,
      "last_error": null,
      "expected_exports": [
        "registerSiteBuilderColorPalettesRoutes"
      ],
      "route": {
        "method": "GET",
        "path": "/api/site-builder/color-palettes"
      },
      "revive_count": 6,
      "revived_at": "2026-07-15T01:55:24.746Z",
      "commit_sha": null,
      "built_sha": null,
      "proof": null,
      "last_attempt": null,
      "last_attempt_at": "2026-07-15T01:55:27.679Z",
      "demoted": false,
      "demote_reason": null,
      "demoted_at": null,
      "park_until": null,
      "no_op": null,
      "pre_existing": true,
      "blocker_class": null,
      "claim_level": null,
      "blocker_type": null,
      "blocker_resolution": null,
      "blueprint_id": "PRODUCT-SITE-BUILDER-BUILD-QUEUE-TWIN-V1",
      "blueprint_step_id": "step-04",
      "mission_id": "PRODUCT-site-builder",
      "shipped_via": "pre_existing_artifact_proof",
      "shipped_at": "2026-07-20T04:44:07.255Z"
    },
    {
      "id": "step-04-register",
      "status": "done",
      "target_file": "config/auto-registered-product-modules.json",
      "task": "Auto-register site-builder color palettes routes at boot without editing server.js",
      "spec": "Add {\"path\":\"routes/site-builder-color-palettes-routes.js\",\"register\":\"registerSiteBuilderColorPalettesRoutes\",\"enabled\":true} to modules[]. Preserve all existing entries. File must remain valid JSON.",
      "depends_on": [
        "step-04"
      ],
      "founder_gated": false,
      "attempts": 0,
      "shipped_via": "governed_ship_queue",
      "shipped_at": "2026-07-14T17:41:46.350Z",
      "blueprint_id": "PRODUCT-SITE-BUILDER-BUILD-QUEUE-TWIN-V1",
      "blueprint_step_id": "step-04-register",
      "mission_id": "PRODUCT-site-builder"
    },
    {
      "id": "sb-deliverability-gate-v2",
      "status": "blocked",
      "target_file": "services/site-builder-deliverability-check.js",
      "task": "Workaround for sb-deliverability-gate (superseded): same DNS-based SPF/DKIM deliverability check, targeting a brand-new file instead of appending to services/site-builder-prospect-runner.js, to avoid a confirmed platform bug where the builder fails to produce a real export when appending a new function to a file ending in an export default {} object (10/10 confirmed failures on the old target).",
      "spec": "Create a brand-new file (this file does not exist yet -- greenfield, no existing content to merge with, no export default {} object anywhere in it, which is the deliberate point: a prior 10-attempt effort to add this same function to services/site-builder-prospect-runner.js -- which DOES end in an export default {} object -- failed every single time with the function only being referenced inside that object instead of declared as a real export, despite maximally explicit specs; this is a workaround that avoids the trigger condition entirely rather than another attempt at the same pattern).\n\nThe file's ENTIRE content should be just this one function, exported directly:\n\nexport async function checkDomainDeliverabilityDNS(domain, { dkimSelector = 'default', resolveTxtFn } = {}) {\n  const resolve = resolveTxtFn || ((name) => import('node:dns').then((dns) => dns.promises.resolveTxt(name)));\n  if (!domain) {\n    return { deliverabilityReady: false, deliverabilityBlockers: ['missing_domain'] };\n  }\n  const blockers = [];\n  let spfFound = false;\n  let dkimFound = false;\n  try {\n    const spfRecords = await resolve(domain);\n    spfFound = spfRecords.some((rec) => rec.join('').toLowerCase().includes('v=spf1'));\n  } catch (err) {\n    blockers.push(`spf_lookup_failed:${err.code || err.message}`);\n  }\n  if (!spfFound) blockers.push('spf_record_not_found');\n  try {\n    const dkimRecords = await resolve(`${dkimSelector}._domainkey.${domain}`);\n    dkimFound = dkimRecords.some((rec) => rec.join('').toLowerCase().includes('v=dkim1'));\n  } catch (err) {\n    blockers.push(`dkim_lookup_failed:${err.code || err.message}`);\n  }\n  if (!dkimFound) blockers.push('dkim_record_not_found');\n  return {\n    deliverabilityReady: spfFound && dkimFound,\n    deliverabilityBlockers: blockers,\n  };\n}\n\nAdd this JSDoc block as the very first thing in the file, before the function: /**\n * SYNOPSIS: Exports checkDomainDeliverabilityDNS — services/site-builder-deliverability-check.js.\n * @ssot docs/products/site-builder/PRODUCT_HOME.md\n */\n\nThat is the entire file -- nothing else. Do not add an export default object. Do not import or reference services/site-builder-prospect-runner.js from this file (wiring the two together is a separate, later step). resolveTxtFn is dependency-injected for testability; defaults to a real node:dns lookup. Must fail closed (deliverabilityReady: false with a specific blocker string) on any error -- never fail open, never throw.",
      "depends_on": [],
      "founder_gated": false,
      "attempts": 1,
      "expected_exports": [
        "checkDomainDeliverabilityDNS"
      ],
      "behavior_assertions": [
        {
          "type": "exports_smoke",
          "path": "services/site-builder-deliverability-check.js",
          "exports": [
            "checkDomainDeliverabilityDNS"
          ],
          "assertion_id": "expected_exports:services/site-builder-deliverability-check.js"
        }
      ],
      "blueprint_id": "PRODUCT-SITE-BUILDER-BUILD-QUEUE-TWIN-V1",
      "blueprint_step_id": "sb-deliverability-gate-v2",
      "mission_id": "PRODUCT-site-builder",
      "last_attempt_at": "2026-07-21T20:20:19.477Z",
      "last_error": "NOT_ON_BLUEPRINT",
      "commit_sha": null,
      "built_sha": null,
      "proof": null,
      "failure_signature": "NOT_ON_BLUEPRINT",
      "same_signature_count": 1,
      "escalation_class": "governance_block"
    }
  ],
  "sentry_signature": "beba57e366c8c9cf",
  "sentry_unplannable_at": "2026-07-12T18:05:57.513Z",
  "sentry_unplannable_reason": "planner filtered all steps (already-done); spin-break so LifeOS can build",
  "blueprint_id": "PRODUCT-SITE-BUILDER-BUILD-QUEUE-TWIN-V1",
  "mission_id": "PRODUCT-site-builder",
  "twin_kind": "product_build_queue_registered"
}
