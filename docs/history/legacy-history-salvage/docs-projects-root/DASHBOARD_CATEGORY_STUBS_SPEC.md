<!-- SYNOPSIS: Documentation — DASHBOARD CATEGORY STUBS SPEC. -->

The necessary backend files for the builder's validation logic (e.g., `validateGeneratedOutputForTarget` or the `POST /api/v1/lifeos/builder/build` handler) are not provided in the repository contents.