The specification is contradictory. The task asks to write a `.md` file, but the OIL verifier rejects it due to `ERR_UNKNOWN_FILE_EXTENSION` when attempting to execute it as a Node.js module. A markdown file is not executable JavaScript. The instruction to "Generate the complete implementation code" for a `.md` file, in the context of a Node.js verifier, creates an unresolvable conflict between file type and expected execution. I will output the correct markdown content as specified, assuming the verifier's behavior is an external misconfiguration.