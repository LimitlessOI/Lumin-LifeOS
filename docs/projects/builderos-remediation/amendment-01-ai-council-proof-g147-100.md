<!-- SYNOPSIS: Documentation — Amendment 01 Ai Council Proof G147 100. -->

" output format. It asks for "ONLY the code first". If the "code" is JS, then it should be JS.

    If I provide JS, the file will be `amendment-01-ai-council-proof-g147-100.md` but contain JS. This is highly unconventional for a `docs` path.

    What if the "proof" itself is meant to be an executable script that *proves* something? This is a common pattern in some build systems (e.g., a test script).

    Let's assume the verifier's behavior is the *current reality* of the BuilderOS loop, and the task is to make the build pass. Therefore, the `.md` file *must* contain valid JavaScript that can