### Blueprint Proof: G106-100 - Core Command Definition and Registry

**1. Exact missing implementation or proof gap:**
The foundational capability to define, register, and subsequently retrieve command definitions is missing. This includes the core data structures for `CommandDefinition` and `CommandInstance`, and the `CommandRegistry` component to manage these definitions. Without this, no commands can be understood