# Amendment 16 Word Keeper Proof - G18-100 Remediation Note

This document serves as a proof-closing blueprint note for the Word Keeper feature, addressing the identified implementation gaps and outlining the next smallest build slice.

## 1. Exact Missing Implementation or Proof Gap

The core gap is the incomplete and unverified persistence layer definition for the `WordKeeperEntry` model. The existing `prisma/schema.prisma` snippet for `WordKeeperEntry` is syntactically incomplete, specifically the `updated` field is missing its type and decorator (e.g., `updatedAt DateTime @updatedAt`). This incomplete definition has not been fully integrated or validated against the database schema.

## 2. Smallest Safe Build Slice to Close It

The smallest safe build slice involves completing the `WordKeeperEntry` model definition within `prisma/schema.prisma` to a valid state, ensuring it can be successfully migrated and generate a functional Prisma client. This establishes the foundational data structure for the Word Keeper feature.

## 3. Exact Safe-Scope Files to Touch First

- `prisma/schema.prisma`: To complete the `WordKeeperEntry` model definition.
- `prisma/migrations/<timestamp>_init_word_keeper.sql` (or similar): Generated by Prisma CLI after `prisma migrate dev` or `prisma db push`.

## 4. Verifier/Runtime Checks

1.  **Prisma Schema Validation:** Run `npx prisma validate` to ensure the `schema.prisma` file is syntactically correct.
2.  **Database Migration:** Execute `npx prisma migrate dev --name init_word_keeper` (or `npx prisma db push` for development) to apply the schema changes to the database.
3.  **Prisma Client Generation:** Run `npx prisma generate` to update the Prisma client.
4.  **Client Usability Check:** In a temporary script or test, attempt to import the Prisma client and perform a basic operation on `prisma.wordKeeperEntry`, e.g., `await prisma.wordKeeperEntry.count()`. This verifies the client is generated correctly and can interact with the new table.

## 5. Stop Conditions if Runtime Truth Disagrees

-   `npx prisma validate` reports errors.
-   `npx prisma migrate dev` or `npx prisma db push` fails to apply changes or reports schema conflicts.
-   `npx prisma generate` fails to produce the client.
-   Attempting to use `prisma.wordKeeperEntry` results in a TypeScript error (e.g., `Property 'wordKeeperEntry' does not exist on type 'PrismaClient'`) or a runtime database error.
-   The database table `WordKeeperEntry` (or `word_keeper_entry`) is not created or does not have the expected columns.