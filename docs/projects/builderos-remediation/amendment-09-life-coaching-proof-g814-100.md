<!-- SYNOPSIS: Documentation — Amendment 09 Life Coaching Proof G814 100. -->

AMENDMENT 09: Life Coaching Integration - Proof G814-100
Blueprint Note: Core Data Model - CoachingSession Definition

This note closes the proof for the initial data model definition for `CoachingSession`, marking the first concrete implementation step for Amendment 09.

1. Exact Missing Implementation or Proof Gap
The foundational data model and schema definition for the `CoachingSession` entity are currently undefined. This gap prevents any subsequent apiEP development or frontend integration related to scheduling and managing coaching sessions.

2. Smallest Safe Build Slice to Close It
Define the `CoachingSession` data model, including its schema, validation rules, and relationships, and implement the corresponding db migration to create the `coaching_sessions` table. This slice establishes the core entity without affecting existing user features or customer-facing surfaces.

3. Exact Safe-Scope Files to Touch First
-   `src/models/CoachingSession.js`: New file for the Node/ESM model definition
-   `src/db/migrations/YYYYMMDDHHMMSS-create-coaching-sessions.js`: New file for the database migration script. (Note: YYYYMMDDHHMMSS will be a timestamp generated by the migration tool.)

4. Verifier/Runtime Checks
-   **Database Schema Verification**:
    -   Execute `npm run db:migrate` (or equivalent command to apply migrations).
    -   Connect to the database (e.g., via `psql` or a database client) and verify the existence of the `coaching_sessions` table.
    -   Inspect the table schema to confirm the presence of expected columns (e.g., `id`, `coachId`, `clientId`, `scheduledAt`, `status`, `createdAt`, `updatedAt`) and their correct data types and constraints (e.g., foreign keys, non-nullability).
-   **Model Basic CRUD Test**:
    -   Write a temporary, isolated test script (e.g., `scripts/test-coaching-session-model.js`) or use a Node.js REPL.
    -   Import the `CoachingSession` model from `src/models/CoachingSession.js`.
    -   Attempt to create a new `CoachingSession` instance with valid data.
    -   Persist the instance to the database.
    -   Retrieve the instance by its ID.
    -   Verify that the retrieved instance's properties match the created data.
    -   Attempt to update and delete the instance.

5. Stop Conditions if Runtime Truth Disagrees
-   **Migration Failure**: If `npm run db:migrate` fails, or the `coaching_sessions` table is not created, or its schema is incorrect:
    -   **STOP** the build process immediately.
    -   Rollback the migration.
    -   Investigate and debug the `src/models/CoachingSession.js` file for schema definition errors, or the migration script (`src/db/migrations/YYYYMMDDHHMMSS-create-coaching-sessions.js`) for syntax or logic issues.
-   **Model CRUD Failure**: If the basic CRUD operations (create, read, update, delete) for `CoachingSession` instances fail, or data integrity issues are observed (e.g., validation errors, incorrect data types persisted):
    -   **STOP** the build process immediately.
    -   Investigate and debug `src/models/CoachingSession.js` for incorrect model definitions, validation rules, or issues with its interaction layer with the database.
    -   Verify the database connection configuration and user permissions.