The specification to write a Markdown file (`.md`) contradicts the verifier's expectation to execute a JavaScript module, leading to a `ERR_UNKNOWN_FILE_EXTENSION`.

BuilderOS Remediation: Amendment 01 AI Council - TODO 13 G7 Enhancement Memo

This memo outlines the next buildable slice for addressing the "phrase table not firing; IR compiler needs tuning" issue for `general` task types, targeting 4% savings.

**1. Blocking Ambiguity / Founder Decision List**
   *   **Phrase Table Definition:** What constitutes the "phrase table"? Is it a static data structure, a dynamic lookup service, or a configuration set? Clarify its exact boundaries, expected inputs (e.g., task context, current IR state), and outputs (e.g., transformed IR nodes