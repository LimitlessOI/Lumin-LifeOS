{
  "project_id": "project_governance",
  "name": "SSOT Project Governance System",
  "amendment_path": "docs/projects/AMENDMENT_18_PROJECT_GOVERNANCE.md",
  "lifecycle": "production",
  "status": "active",
  "priority": "high",
  "owner": "adam",
  "phase": "core-infrastructure",
  "next_task": "Upgrade remaining amendments (02-11, 13-16) to full template",
  "stability_class": "safe",
  "current_focus": "Governance infrastructure live — seed data, C&C panels, CI pipeline",
  "owned_files": [
    "routes/project-governance-routes.js",
    "scripts/verify-project.mjs",
    "scripts/ssot-staleness-check.mjs",
    "scripts/check-coupling.mjs",
    "scripts/seed-projects.mjs",
    "db/migrations/20260327_project_governance.sql",
    "docs/projects/manifest.schema.json",
    "docs/projects/AMENDMENT_TEMPLATE.md",
    "policy/ssot-coupling.rego",
    ".github/workflows/ssot-compliance.yml",
    ".github/CODEOWNERS"
  ],
  "protected_files": [
    "server.js",
    "startup/register-runtime-routes.js",
    "CLAUDE.md"
  ],
  "required_files": [
    "routes/project-governance-routes.js",
    "scripts/verify-project.mjs",
    "db/migrations/20260327_project_governance.sql"
  ],
  "required_routes": [
    { "method": "GET", "path": "/api/v1/projects", "expected_status": 200, "description": "Projects list" },
    { "method": "GET", "path": "/api/v1/pending-adam", "expected_status": 200, "description": "Pending Adam items" }
  ],
  "required_tables": [
    { "name": "projects", "required_columns": ["id", "slug", "name", "lifecycle", "status"] },
    { "name": "project_segments", "required_columns": ["id", "project_id", "title", "status", "estimated_hours"] },
    { "name": "estimation_log", "required_columns": ["id", "project_id", "segment_id", "accuracy_pct"] },
    { "name": "pending_adam", "required_columns": ["id", "project_id", "title", "type", "priority", "is_resolved"] }
  ],
  "required_env": [
    "DATABASE_URL",
    "COMMAND_CENTER_KEY"
  ],
  "assertions": [
    { "type": "node_check", "check": "routes/project-governance-routes.js", "label": "governance routes syntax" },
    { "type": "node_check", "check": "scripts/verify-project.mjs", "label": "verifier syntax" },
    { "type": "file_exists", "check": "db/migrations/20260327_project_governance.sql", "label": "governance migration exists" },
    { "type": "file_exists", "check": ".github/CODEOWNERS", "label": "CODEOWNERS exists" },
    { "type": "file_exists", "check": ".github/workflows/ssot-compliance.yml", "label": "CI workflow exists" }
  ],
  "completion_checks": [
    "All 4 active projects seeded in DB with segments",
    "Projects Dashboard renders in C&C with progress bars",
    "Pending Adam panel shows open items and one-click resolve",
    "verify-project.mjs runs and writes pass/fail to DB",
    "ssot-staleness-check.mjs detects code newer than amendment",
    "Estimation accuracy auto-tracked when segments marked done",
    "CI ssot-compliance.yml passes on every commit"
  ],
  "anti_drift_notes": "Router mounted ONCE at /api/v1 in register-runtime-routes.js — not 3× at sub-paths. All route handlers use full sub-paths like router.get('/projects'). Seed script uses delete-before-insert for segments (no unique constraint). estimation_log has no ON CONFLICT support — avoid duplicate inserts.",
  "decision_debt": [
    { "description": "No unique constraint on project_segments(project_id, title) — seed compensates with delete-first", "resolved": false },
    { "description": "Remaining amendments 02-11, 13-16 not yet upgraded to full template", "resolved": false }
  ],
  "last_verified_at": null,
  "verification_passed": null
}
