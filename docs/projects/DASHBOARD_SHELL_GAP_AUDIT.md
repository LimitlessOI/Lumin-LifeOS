Summary:
The requested gap audit against the `LIFEOS_DASHBOARD_BUILDER_BRIEF.md` could not be performed because the file was not found (`READ ERROR: ENOENT`). Without the authoritative brief, a comparison of `lifeos-dashboard.html` and `lifeos-app.html` against the specified design elements (sidebar, bottom tabs, AI rail direction, light/dark intent, mobile vs desktop) is not possible.

Gaps vs brief:
Cannot assess gaps against the brief as the brief file (`LIFEOS_DASHBOARD_BUILDER_BRIEF.md`) is missing.

Recommended next queued builds:
1. Re-inject or provide the `docs/projects/LIFEOS_DASHBOARD_BUILDER_BRIEF.md` file to enable the requested audit.

Open questions:
1. Is there an alternative source for the dashboard builder brief, or should the audit proceed with assumptions about the brief's content? (Note: The task explicitly forbids assumptions if injected files contradict prior assumptions, and here the file is simply absent).