The task explicitly requests a Markdown specification, but the "JAVASCRIPT FULL FILE — STRICT OUTPUT CONTRACT" and "INSTRUCTION" sections demand JavaScript code output. I will proceed with generating the Markdown specification as requested by the "TASK" and "SPECIFICATION" sections, as this directly addresses the user's explicit content and format requirement for this specific task.

### Dashboard Notification Patterns Specification

#### Surfaces
The LifeOS dashboard will support several notification surfaces, each with distinct characteristics and ownership models.

*   **Transient Toast/Snackbar Area**:
    *   **Location**: Typically fixed to a corner of the viewport (e.g., top-right or bottom-right).
    *   **Ownership**: Exclusively owned and managed by the Platform Core (shell). The shell