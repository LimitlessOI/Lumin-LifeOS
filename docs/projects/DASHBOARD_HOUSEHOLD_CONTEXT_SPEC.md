<!DOCTYPE html>
<html lang="en">
<head> <meta charset="UTF-8"> <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover"> <script src="/overlay/lifeos-bootstrap.js"></script> <script src="/overlay/lifeos-theme.js"></script> <script src="/overlay/lifeos-feature-data.js"></script> <meta name="mobile-web-app-capable" content="yes"> <meta name="apple-mobile-web-app-capable" content="yes"> <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent"> <meta name="apple-mobile-web-app-title" content="LifeOS"> <meta name="theme-color" content="#0a0a0f"> <link rel="manifest" href="/overlay/lifeos.webmanifest"> <link rel="apple-touch-icon" href="/overlay/icons/icon-192.png"> <title>LifeOS</title> <style> / ── Design tokens (mirrors lifeos-ds.css — shell uses its own copy since it IS the chrome) ── / :root { --bg-base: #0a0a0f; --bg-raised: #0d0d15; --bg-surface: #111118; --bg-surface2: #17171f; --bg-overlay: #1e1e28; --border: rgba(255,255,255,0.07); --border-focus: rgba(255,255,255,0.15); --text-primary: #e8e8f0; --text-secondary:#9999bb; --text-muted: #555566; / Section accent colors / --c-today: #5b6af5; --c-mirror: #7c3aed; --c-engine: #3b82f6; --c-health: #10b981; --c-inner: #f59e0b; --c-family: #f43f5e; --c-purpose: #8b5cf6; --c-children: #f5a623; --c-mediation: #14b8a6; --c-conflict: #e05555; --c-balance: #5b6af5; --c-coach: #5b6af5; --c-identity: #a78bfa; --c-decisions: #f59e0b; --c-growth: #34d399; --c-finance: #22d3ee; --c-vision: #38bdf8; --c-legacy: #fb923c; --c-healing: #c084fc; --c-dashboard: #6366f1; --c-victory: #fbbf24; / Layout / --sidebar-w: 240px; --sidebar-w-mini: 60px; --topbar-h: 52px; --bottomnav-h: 56px; / Safe areas / --safe-top: env(safe-area-inset-top, 0px); --safe-right: env(safe-area-inset-right, 0px); --safe-bottom: env(safe-area-inset-bottom, 0px); --safe-left: env(safe-area-inset-left, 0px); / Current section (updated by JS) / --section-color: var(--c-today); --radius-sm: 6px; --radius-md: 10px; --radius-lg: 14px; --transition: 0.2s ease; } html[data-theme="light"] { color-scheme: light; --bg-base: #f6f7fb; --bg-raised: #ffffff; --bg-surface: #ffffff; --bg-surface2: #f3f5fb; --bg-overlay: #ffffff; --border: rgba(15,23,42,0.12); --border-focus: rgba(91,106,245,0.32); --text-primary: #111827; --text-secondary:#475569; --text-muted: #64748b; } , ::before, *::after { box-sizing: border-box; margin: 0; padding: 0; } html { font-size: 16px; -webkit-text-size-adjust: 100%; height: 100%; } body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', system-ui, sans-serif; background: var(--bg-base); color: var(--text-primary); height: 100%; overflow: hidden; -webkit-font-smoothing: antialiased; } / ── App shell ── / .app { display: flex; height: 100svh; / svh: respects mobile browser chrome / padding-top: var(--safe-top); } /* ═══════════════════════════════════════════════════════ SIDEBAR ═══════════════════════════════════════════════════════ */ .sidebar { width: var(--sidebar-w); background: var(--bg-raised); border-right: 1px solid var(--border); display: flex; flex-direction: column; flex-shrink: 0; overflow: hidden; transition: width var(--transition); position: relative; z-index: 50; } .sidebar.mini { width: var(--sidebar-w-mini); } / Sidebar top: logo row / .sidebar-header { height: var(--topbar-h); padding: 0 16px; border-bottom: 1px solid var(--border); display: flex; align-items: center; justify-content: space-between; flex-shrink: 0; gap: 8px; } .logo { font-size: 15px; font-weight: 700; color: var(--text-primary); letter-spacing: -0.4px; white-space: nowrap; display: flex; align-items: center; gap: 8px; } .logo-icon { width: 26px; height: 26px; border-radius: 7px; background: linear-gradient(135deg, var(--c-today), var(--c-mirror)); display: flex; align-items: center; justify-content: center; font-size: 13px; flex-shrink: 0; } .logo-text { color: var(--text-primary); } .logo-text em { color: var(--c-today); font-style: normal; } .sidebar.mini .logo-text { display: none; } .collapse-btn { background: none; border: none; color: var(--text-muted); cursor: pointer; padding: 6px; border-radius: var(--radius-sm); line-height: 1; font-size: 12px; flex-shrink: 0; transition: color var(--transition), background var(--transition); min-width: 28px; min-height: 28px; display: flex; align-items: center; justify-content: center; } .collapse-btn:hover { color: var(--text-primary); background: var(--bg-overlay); } / User pill / .user-pill { padding: 10px 14px; border-bottom: 1px solid var(--border); display: flex; align-items: center; gap: 10px; cursor: pointer; transition: background var(--transition); position: relative; flex-shrink: 0; } .user-pill:hover { background: var(--bg-surface); } .avatar { width: 32px; height: 32px; min-width: 32px; border-radius: 50%; background: linear-gradient(135deg, #2a2060, #3a1a60); border: 1.5px solid rgba(91,106,245,0.4); display: flex; align-items: center; justify-content: center; font-size: 13px; font-weight: 700; color: #a0aaff; } .user-info { overflow: hidden; flex: 1; min-width: 0; } .user-name { font-size: 13px; font-weight: 600; color: var(--text-primary); white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
/* NEW: User context display */
.user-context {
    font-size: 11px;
    color: var(--text-secondary);
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
    margin-top: 2px; /* Small spacing */
    display: flex;
    align-items: center;
    gap: 4px;
}
.user-context::before {
    content: '•'; /* Small dot badge */
    color: var(--c-today); /* Accent color */
    font-size: 14px;
    line-height: 1;
}
.sidebar.mini .user-context {
    display: none;
}
.user-handle { font-size: 11px; color: var(--text-muted); white-space: nowrap; } .chevron { font-size: 10px; color: var(--text-muted); flex-shrink: 0; transition: transform var(--transition); } .sidebar.mini .user-info, .sidebar.mini .chevron { display: none; } / User dropdown / .user-dropdown { position: absolute; top: calc(100% + 4px); left: 8px; right: 8px; background: var(--bg-overlay); border: 1px solid var(--border-focus); border-radius: var(--radius-md); z-index: 200; display: none; box-shadow: 0 8px 24px rgba(0,0,0,0.5); overflow: hidden; } .user-dropdown.open { display: block; } .dd-item { padding: 11px 14px; font-size: 13px; color: var(--text-secondary); cursor: pointer; transition: background var(--transition), color var(--transition); display: flex; align-items: center; gap: 8px; } .dd-item:not(:last-child) { border-bottom: 1px solid var(--border); } .dd-item:hover { background: var(--bg-surface2); color: var(--text-primary); }
/* NEW: Nested dropdown for context picker */
.chevron-right {
    margin-left: auto;
    font-size: 10px;
    color: var(--text-muted);
    transition: transform var(--transition);
}
.context-picker-dropdown {
    display: none; /* Hidden by default */
    background: var(--bg-surface2); /* Slightly different background */
    border-top: 1px solid var(--border);
    padding: 8px 0;
}
.context-picker-dropdown.open {
    display: block;
}
.context-item {
    padding: 8px 14px 8px 24px; /* Indent slightly */
    font-size: 13px;
    color: var(--text-secondary);
    cursor: pointer;
    transition: background var(--transition), color var(--transition);
    display: flex;
    align-items: center;
    gap: 8px;
}
.context-item:hover {
    background: var(--bg-surface);
    color: var(--text-primary);
}
.context-item.active {
    color: var(--c-today);
    font-weight: 600;
    background: rgba(91,106,245,0.08);
}
.context-item-icon {
    font-size: 14px;
    width: 18px;
    text-align: center;
    flex-shrink: 0;
}
/ Nav scroll / .nav-scroll { flex: 1; overflow-y: auto; overflow-x: hidden; padding: 8px 0 16px; scrollbar-width: none; } .nav-scroll::-webkit-scrollbar { display: none; } / Nav group label / .nav-group-label { font-size: 10px; font-weight: 600; letter-spacing: 0.08em; text-transform: uppercase; color: var(--text-muted); padding: 12px 16px 4px; white-space: nowrap; overflow: hidden; transition: opacity var(--transition); } .sidebar.mini .nav-group-label { opacity: 0; pointer-events: none; } .nav-divider { height: 1px; background: var(--border); margin: 6px 12px; } / Nav items / .nav-item { display: flex; align-items: center; gap: 10px; padding: 0 14px; height: 40px; cursor: pointer; transition: background var(--transition), color var(--transition); border-left: 2px solid transparent; text-decoration: none; color: var(--text-muted); font-size: 13px; white-space: nowrap; overflow: hidden; position: relative; user-select: none; } .nav-item:hover { background: var(--bg-surface); color: var(--text-secondary); } .nav-item.active { color: var(--text-primary); background: rgba(255,255,255,0.04); } .nav-icon { font-size: 15px; flex-shrink: 0; width: 20px; text-align: center; line-height: 1; filter: saturate(0.6); transition: filter var(--transition); } .nav-item.active .nav-icon, .nav-item:hover .nav-icon { filter: saturate(1); } .nav-label { flex: 1; overflow: hidden; text-overflow: ellipsis; font-weight: 500; } .sidebar.mini .nav-label { display: none; } .sidebar.mini .nav-item { padding: 0; height: 44px; justify-content: center; } .sidebar.mini .nav-item.active { border-left-color: transparent; } / Tooltip on mini sidebar / .sidebar.mini .nav-item::after { display: none; } .feature-help-popover { position: fixed; width: 320px; max-width: calc(100vw - 24px); background: var(--bg-overlay); border: 1px solid var(--border-focus); border-radius: 14px; padding: 14px; z-index: 950; box-shadow: 0 18px 36px rgba(0,0,0,0.45); opacity: 0; transform: translateY(4px); pointer-events: none; transition: opacity var(--transition), transform var(--transition); } .feature-help-popover.show { opacity: 1; transform: translateY(0); pointer-events: auto; } .feature-help-title { font-size: 14px; font-weight: 700; color: var(--text-primary); margin-bottom: 8px; } .feature-help-summary { font-size: 13px; line-height: 1.5; color: var(--text-secondary); margin-bottom: 10px; } .feature-help-list { display: grid; gap: 6px; margin-bottom: 12px; } .feature-help-list div { font-size: 12px; line-height: 1.45; color: var(--text-primary); padding: 8px 10px; background: var(--bg-surface); border: 1px solid var(--border); border-radius: 10px; } .feature-help-link { display: inline-flex; align-items: center; justify-content: center; min-height: 36px; padding: 0 12px; border-radius: 10px; border: 1px solid var(--border-focus); color: var(--text-primary); background: var(--bg-surface2); text-decoration: none; font-size: 12px; font-weight: 600; cursor: pointer; } .feature-help-link:hover { background: var(--bg-surface); } @media (max-width: 999px) { .feature-help-popover { display: none; } } /* ═══════════════════════════════════════════════════════ MAIN AREA ═══════════════════════════════════════════════════════ */ .main { flex: 1; display: flex; flex-direction: column; overflow: hidden; min-width: 0; } / Desktop topbar / .topbar { height: var(--topbar-h); border-bottom: 1px solid var(--border); display: flex; align-items: center; padding: 0 20px; gap: 12px; flex-shrink: 0; background: var(--bg-raised); position: relative; } / Section color accent line at bottom of topbar / .topbar::after { content: ''; position: absolute; bottom: 0; left: 0; right: 0; height: 2px; background: var(--section-color); opacity: 0.6; transition: background 0.3s ease; } .topbar-icon { font-size: 16px; line-height: 1; } .topbar-title { font-size: 14px; font-weight: 600; color: var(--text-primary); flex: 1; letter-spacing: -0.01em; } .topbar-actions { display: flex; gap: 4px; } .topbar-btn { background: none; border: none; color: var(--text-muted); font-size: 16px; cursor: pointer; padding: 0; width: 36px; height: 36px; border-radius: var(--radius-sm); display: flex; align-items: center; justify-content: center; transition: color var(--transition), background var(--transition); } .topbar-btn:hover { color: var(--text-primary); background: var(--bg-surface); } / Content iframe / .content-frame { flex: 1; width: 100%; border: none; background: var(--bg-base); display: block; } / One-tap Lumin entry — visible companion strip (not only ◎ / FAB) / .lumin-quick-bar { flex-shrink: 0; display: flex; align-items: center; gap: 10px; width: 100%; margin: 0; padding: 10px 14px 11px; border: none; border-bottom: 1px solid var(--border); background: linear-gradient(180deg, rgba(91, 106, 245, 0.08), var(--bg-surface2)); cursor: pointer; text-align: left; font: inherit; color: inherit; transition: background 0.2s, box-shadow 0.2s; -webkit-tap-highlight-color: transparent; } .lumin-quick-bar:hover, .lumin-quick-bar:focus-visible { background: var(--bg-surface); outline: none; } .lumin-quick-bar.active { box-shadow: inset 0 2px 0 0 rgba(91, 106, 245, 0.55); } .lumin-quick-icon { width: 34px; height: 34px; border-radius: 10px; background: linear-gradient(135deg, var(--c-today), var(--c-mirror)); color: #fff; font-size: 16px; display: flex; align-items: center; justify-content: center; flex-shrink: 0; } .lumin-quick-copy { flex: 1; min-width: 0; display: flex; flex-direction: column; gap: 2px; } .lumin-quick-title { font-size: 13px; font-weight: 600; color: var(--text-primary); letter-spacing: -0.02em; } .lumin-quick-sub { font-size: 11px; color: var(--text-muted); line-height: 1.35; } .lumin-quick-chev { color: var(--text-muted); font-size: 18px; flex-shrink: 0; } @media (max-width: 420px) { .lumin-quick-sub { display: none; } } /* ═══════════════════════════════════════════════════════ MOBILE CHROME ═══════════════════════════════════════════════════════ */ / Mobile top bar / .mobile-topbar { display: none; height: var(--topbar-h); background: var(--bg-raised); border-bottom: 1px solid var(--border); align-items: center; padding: 0 8px 0 4px; gap: 4px; flex-shrink: 0; position: relative; } .mobile-topbar::after { content: ''; position: absolute; bottom: 0; left: 0; right: 0; height: 2px; background: var(--section-color); opacity: 0.6; transition: background 0.3s ease; } .mobile-menu-btn { background: none; border: none; color: var(--text-secondary); font-size: 18px; cursor: pointer; min-width: 44px; min-height: 44px; display: flex; align-items: center; justify-content: center; border-radius: var(--radius-sm); } .mobile-logo { font-size: 15px; font-weight: 700; color: var(--text-primary); flex: 1; display: flex; align-items: center; gap: 7px; letter-spacing: -0.3px; } .mobile-logo-icon { width: 24px; height: 24px; border-radius: 6px; background: linear-gradient(135deg, var(--c-today), var(--c-mirror)); display: flex; align-items: center; justify-content: center; font-size: 12px; } .mobile-section-name { font-size: 13px; font-weight: 600; color: var(--text-primary); } / Mobile bottom nav / .mobile-bottomnav { display: none; background: var(--bg-raised); border-top: 1px solid var(--border); flex-shrink: 0; / Safe area padding for home indicator / padding-bottom: var(--safe-bottom); } .mob-tab-row { display: flex; height: var(--bottomnav-h); } .mob-tab { flex: 1; display: flex; flex-direction: column; align-items: center; justify-content: center; cursor: pointer; transition: background var(--transition); gap: 3px; min-height: 44px; -webkit-tap-highlight-color: transparent; position: relative; } .mob-tab::before { content: ''; position: absolute; top: 0; left: 8px; right: 8px; height: 2px; background: transparent; border-radius: 0 0 2px 2px; transition: background var(--transition); } .mob-icon { font-size: 18px; line-height: 1; filter: saturate(0.4) brightness(0.7); transition: filter var(--transition); } .mob-label { font-size: 10px; color: var(--text-muted); font-weight: 500; white-space: nowrap; transition: color var(--transition); } .mob-tab.active .mob-icon { filter: saturate(1) brightness(1); } .mob-tab.active .mob-label { color: var(--text-secondary); } / ── More bottom sheet ── / .more-sheet-backdrop { display: none; position: fixed; inset: 0; background: rgba(0,0,0,0.5); z-index: 400; -webkit-backdrop-filter: blur(2px); backdrop-filter: blur(2px); } .more-sheet-backdrop.open { display: block; } .more-sheet { position: fixed; bottom: 0; left: 0; right: 0; background: var(--bg-raised); border-top: 1px solid var(--border-focus); border-radius: 18px 18px 0 0; z-index: 500; transform: translateY(100%); transition: transform 0.3s cubic-bezier(0.4, 0, 0.2, 1); padding-bottom: var(--safe-bottom); max-height: 70svh; overflow-y: auto; } .more-sheet.open { transform: translateY(0); } .more-sheet-handle { width: 36px; height: 4px; background: var(--border-focus); border-radius: 2px; margin: 10px auto 16px; } .more-sheet-title { font-size: 11px; font-weight: 600; letter-spacing: 0.06em; text-transform: uppercase; color: var(--text-muted); padding: 0 20px 10px; } .more-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 2px; padding: 0 8px 16px; } .more-item { display: flex; flex-direction: column; align-items: center; padding: 14px 8px; cursor: pointer; border-radius: var(--radius-md); gap: 6px; transition: background var(--transition); -webkit-tap-highlight-color: transparent; } .more-item:hover, .more-item:active { background: var(--bg-surface); } .more-item-icon { font-size: 22px; line-height: 1; } .more-item-label { font-size: 11px; color: var(--text-secondary); white-space: nowrap; font-weight: 500; } /* ═══════════════════════════════════════════════════════ SETTINGS PANEL ═══════════════════════════════════════════════════════ */ .settings-backdrop { position: fixed; inset: 0; background: rgba(0,0,0,0.5); z-index: 600; display: none; -webkit-backdrop-filter: blur(2px); backdrop-filter: blur(2px); } .settings-backdrop.open { display: block; } .settings-panel { position: fixed; top: 0; right: -360px; bottom: 0; width: 320px; max-width: 90vw; background: var(--bg-surface); border-left: 1px solid var(--border-focus); z-index: 700; display: flex; flex-direction: column; transition: right 0.28s cubic-bezier(0.4, 0, 0.2, 1); padding-top: var(--safe-top); } .settings-panel.open { right: 0; } .settings-header { padding: 18px 20px; border-bottom: 1px solid var(--border); display: flex; justify-content: space-between; align-items: center; } .settings-title { font-size: 15px; font-weight: 600; } .settings-close { background: var(--bg-overlay); border: none; color: var(--text-secondary); font-size: 14px; cursor: pointer; width: 32px; height: 32px; border-radius: var(--radius-sm); display: flex; align-items: center; justify-content: center; transition: color var(--transition), background var(--transition); } .settings-close:hover { color: var(--text-primary); background: var(--bg-surface2); } .settings-body { padding: 20px; flex: 1; overflow-y: auto; } .settings-section { margin-bottom: 24px; } .settings-section-title { font-size: 10px; font-weight: 600; letter-spacing: 0.08em; text-transform: uppercase; color: var(--text-muted); margin-bottom: 10px; } .settings-row { margin-bottom: 14px; } .settings-label { font-size: 12px; color: var(--text-secondary); margin-bottom: 5px; font-weight: 500; } .settings-input { width: 100%; background: var(--bg-surface2); border: 1px solid var(--border); border-radius: var(--radius-md); color: var(--text-primary); padding: 10px 12px; font-size: 13px; font-family: inherit; outline: none; min-height: 44px; transition: border-color var(--transition); } .settings-input:focus { border-color: var(--border-focus); } .settings-value { font-size: 13px; color: var(--text-secondary); padding: 4px 0; } .settings-divider { height: 1px; background: var(--border); margin: 4px 0 20px; } .settings-save-btn { width: 100%; padding: 12px; background: var(--c-today); border: none; border-radius: var(--radius-md); color: #fff; font-size: 14px; font-weight: 500; cursor: pointer; font-family: inherit; min-height: 44px; transition: opacity var(--transition); } .settings-save-btn:hover { opacity: 0.88; } .settings-link { color: var(--c-today); text-decoration: none; font-size: 13px; cursor: pointer; } .settings-link:hover { text-decoration: underline; } .settings-hint { font-size: 12px; color: var(--text-secondary); line-height: 1.45; margin: 0 0 12px; } .settings-invite-btn { width: 100%; padding: 10px 12px; border-radius: 8px; border: 1px solid var(--border); background: var(--bg-surface2); color: var(--text-primary); font-size: 13px; font-weight: 600; cursor: pointer; margin-bottom: 12px; } .settings-invite-btn:hover { border-color: var(--border-focus); } .admin-invites-list { max-height: 220px; overflow-y: auto; font-size: 12px; } .admin-invite-row { border: 1px solid var(--border); border-radius: 8px; padding: 10px 10px 8px; margin-bottom: 8px; background: var(--bg-surface); } .admin-invite-row code { font-size: 11px; word-break: break-all; } .admin-invite-meta { color: var(--text-secondary); font-size: 11px; margin-top: 6px; } .admin-invite-actions { display: flex; gap: 8px; margin-top: 8px; flex-wrap: wrap; } .admin-invite-actions button { font-size: 11px; padding: 6px 10px; border-radius: 6px; border: 1px solid var(--border); background: var(--bg-surface2); color: var(--text-primary); cursor: pointer; } .admin-invite-actions button.primary { border-color: var(--c-today); color: var(--c-today); } .theme-toggle-btn { min-height: 36px; padding: 0 12px; border-radius: var(--radius-md); border: 1px solid var(--border); background: var(--bg-surface2); color: var(--text-primary); font-size: 13px; cursor: pointer; transition: background var(--transition), border-color var(--transition); } .theme-toggle-btn:hover { border-color: var(--border-focus); } /* ═══════════════════════════════════════════════════════ TOAST ═══════════════════════════════════════════════════════ */ .toast { position: fixed; bottom: calc(var(--bottomnav-h) + var(--safe-bottom) + 12px); left: 50%; transform: translateX(-50%) translateY(8px); background: var(--bg-overlay); border: 1px solid var(--border-focus); color: var(--text-primary); padding: 10px 18px; border-radius: 20px; font-size: 13px; white-space: nowrap; z-index: 900; opacity: 0; pointer-events: none; box-shadow: 0 4px 20px rgba(0,0,0,0.5); transition: opacity 0.2s, transform 0.2s; } .toast.show { opacity: 1; transform: translateX(-50%) translateY(0); } @media (min-width: 600px) { .toast { bottom: 24px; } } /* ═══════════════════════════════════════════════════════ INSTALL BANNER ═══════════════════════════════════════════════════════ */ #install-banner { position: fixed; bottom: calc(var(--bottomnav-h) + var(--safe-bottom) + 16px); left: 50%; transform: translateX(-50%) translateY(12px); background: linear-gradient(135deg, #1a1860, #1e2a70); border: 1px solid rgba(91,106,245,0.5); border-radius: 14px; padding: 12px 20px 12px 14px; font-size: 13px; color: #c0cbff; z-index: 800; cursor: pointer; white-space: nowrap; box-shadow: 0 8px 28px rgba(0,0,0,0.5); display: flex; align-items: center; gap: 10px; opacity: 0; transition: opacity 0.3s, transform 0.3s; } #install-banner.show { opacity: 1; transform: translateX(-50%) translateY(0); } #install-banner .banner-icon { font-size: 18px; } #install-banner .banner-close { font-size: 12px; color: rgba(160,170,255,0.5); margin-left: 8px; padding: 2px; } / Mobile overlay backdrop (for sidebar drawer) / .mobile-overlay { display: none; position: fixed; inset: 0; background: rgba(0,0,0,0.55); z-index: 490; -webkit-backdrop-filter: blur(1px); backdrop-filter: blur(1px); } .mobile-overlay.open { display: block; } /* ═══════════════════════════════════════════════════════ RESPONSIVE ═══════════════════════════════════════════════════════ */ / 600–999px: icon-only sidebar / @media (min-width: 600px) and (max-width: 999px) { .sidebar { width: var(--sidebar-w-mini); } .sidebar .logo-text, .sidebar .user-info, .sidebar .chevron, .sidebar .nav-label, .sidebar .nav-group-label { display: none; opacity: 0; } .sidebar .nav-item { padding: 0; height: 44px; justify-content: center; } .sidebar .collapse-btn { display: none; } } / Under 600px: full mobile layout / @media (max-width: 599px) { .sidebar { display: none; } .topbar { display: none; } .mobile-topbar { display: flex; } .mobile-bottomnav { display: block; } .sidebar.mobile-open { display: flex; position: fixed; top: 0; left: 0; bottom: 0; width: var(--sidebar-w) !important; z-index: 500; padding-top: var(--safe-top); } } / 1000px+: full sidebar always shown / @media (min-width: 1000px) { .sidebar { width: var(--sidebar-w); } .collapse-btn { display: flex; } .sidebar.mini { width: var(--sidebar-w-mini); } } /* ═══════════════════════════════════════════════════════ LUMIN PERSISTENT DRAWER ═══════════════════════════════════════════════════════ */ / Floating action button / .lumin-fab { position: fixed; bottom: calc(var(--bottomnav-h) + var(--safe-bottom) + 16px); right: 20px; width: 52px; height: 52px; border-radius: 50%; background: linear-gradient(135deg, var(--c-today), var(--c-mirror)); border: none; color: #fff; font-size: 22px; cursor: pointer; z-index: 960; box-shadow: 0 4px 20px rgba(91,106,245,0.4); display: flex; align-items: center; justify-content: center; transition: transform 0.2s, box-shadow 0.2s; -webkit-tap-highlight-color: transparent; } .lumin-fab:hover { transform: scale(1.08); box-shadow: 0 6px 28px rgba(91,106,245,0.55); } .lumin-fab.drawer-open { transform: scale(0.92); } .lumin-fab-badge { position: absolute; top: -2px; right: -2px; width: 14px; height: 14px; background: var(--c-health); border-radius: 50%; border: 2px solid var(--bg-base); display: none; } .lumin-fab-badge.show { display: block; } @media (min-width: 600px) { .lumin-fab { bottom: 24px; } } / Mic active state (voice button in topbar) / #voice-btn.voice-active, #voice-btn-mob.voice-active { color: #ef4444; animation: voice-topbar-pulse 1s infinite; } @keyframes voice-topbar-pulse { 0%, 100% { opacity: 1; } 50% { opacity: 0.5; } } / Drawer backdrop / .lumin-backdrop { position: fixed; inset: 0; background: rgba(0,0,0,0.35); z-index: 970; display: none; -webkit-backdrop-filter: blur(1px); backdrop-filter: blur(1px); } .lumin-backdrop.open { display: block; } / Drawer panel / .lumin-drawer { position: fixed; top: 0; right: -420px; bottom: 0; width: 400px; max-width: 100vw; background: var(--bg-raised); border-left: 1px solid var(--border-focus); z-index: 980; display: flex; flex-direction: column; transition: right 0.28s cubic-bezier(0.4, 0, 0.2, 1); padding-top: var(--safe-top); } .lumin-drawer.open { right: 0; } / Mobile: bottom sheet / @media (max-width: 599px) { .lumin-drawer { top: auto; right: 0; bottom: -100%; left: 0; width: 100%; max-width: 100%; height: 85svh; border-left: none; border-top: 1px solid var(--border-focus); border-radius: 20px 20px 0 0; transition: bottom 0.3s cubic-bezier(0.4, 0, 0.2, 1); } .lumin-drawer.open { bottom: 0; right: 0; } } / Drawer header / .lumin-drawer-header { padding: 14px 16px; border-bottom: 1px solid var(--border); display: flex; align-items: center; gap: 10px; flex-shrink: 0; } .lumin-drawer-avatar { width: 32px; height: 32px; border-radius: 10px; background: linear-gradient(135deg, var(--c-today), var(--c-mirror)); display: flex; align-items: center; justify-content: center; font-size: 15px; flex-shrink: 0; } .lumin-drawer-title { flex: 1; } .lumin-drawer-title strong { font-size: 14px; font-weight: 700; display: block; } .lumin-drawer-title span { font-size: 11px; color: var(--text-muted); } .lumin-drawer-close { background: none; border: none; color: var(--text-muted); font-size: 18px; cursor: pointer; width: 36px; height: 36px; border-radius: var(--radius-sm); display: flex; align-items: center; justify-content: center; transition: color var(--transition), background var(--transition); } .lumin-drawer-close:hover { color: var(--text-primary); background: var(--bg-surface); } .lumin-history-link { font-size: 11px; color: var(--c-today); text-decoration: none; cursor: pointer; padding: 2px 6px; border-radius: 4px; transition: background var(--transition); flex-shrink: 0; } .lumin-history-link:hover { background: var(--bg-surface); } / Voice interim preview / .lumin-interim { padding: 6px 16px; font-size: 12px; color: var(--text-muted); font-style: italic; min-height: 24px; flex-shrink: 0; } / Messages area / .lumin-messages { flex: 1; overflow-y: auto; padding: 12px 16px; display: flex; flex-direction: column; gap: 10px; scrollbar-width: thin; } .lumin-msg { max-width: 85%; padding: 10px 14px; border-radius: 14px; font-size: 13.5px; line-height: 1.55; } .lumin-msg.user { align-self: flex-end; background: var(--c-today); color: #fff; border-bottom-right-radius: 4px; } .lumin-msg.assistant { align-self: flex-start; background: var(--bg-surface); border: 1px solid var(--border); color: var(--text-primary); border-bottom-left-radius: 4px; } .lumin-msg.thinking { align-self: flex-start; background: var(--bg-surface); border: 1px dashed var(--border-focus); color: var(--text-muted); font-style: italic; font-size: 12px; } .lumin-msg-time { font-size: 10px; opacity: 0.55; margin-top: 4px; } / Empty state / .lumin-empty { flex: 1; display: flex; flex-direction: column; align-items: center; justify-content: center; gap: 8px; color: var(--text-muted); font-size: 13px; padding: 20px; text-align: center; } .lumin-empty-icon { font-size: 36px; opacity: 0.5; } / Input bar / .lumin-input-bar { padding: 12px 14px; border-top: 1px solid var(--border); display: flex; align-items: flex-end; gap: 8px; flex-shrink: 0; padding-bottom: calc(12px + var(--safe-bottom)); } .lumin-input-wrap { flex: 1; background: var(--bg-surface); border: 1px solid var(--border); border-radius: 14px; display: flex; align-items: flex-end; transition: border-color var(--transition); } .lumin-input-wrap:focus-within { border-color: var(--border-focus); } .lumin-input { flex: 1; background: none; border: none; color: var(--text-primary); font-family: inherit; font-size: 14px; padding: 10px 12px; resize: none; max-height: 120px; overflow-y: auto; scrollbar-width: none; outline: none; line-height: 1.5; } .lumin-input::placeholder { color: var(--text-muted); } .lumin-input-mic { background: none; border: none; color: var(--text-muted); font-size: 16px; cursor: pointer; padding: 8px 10px 8px 4px; flex-shrink: 0; line-height: 1; transition: color var(--transition); } .lumin-input-mic:hover { color: var(--text-primary); } .lumin-input-mic.voice-active { color: #ef4444; animation: voice-topbar-pulse 0.8s infinite; } .lumin-send-btn { width: 38px; height: 38px; border-radius: 10px; background: var(--c-today); border: none; color: #fff; font-size: 16px; cursor: pointer; display: flex; align-items: center; justify-content: center; flex-shrink: 0; transition: opacity var(--transition); } .lumin-send-btn:hover { opacity: 0.85; } .lumin-send-btn:disabled { opacity: 0.4; cursor: default; } </style> <link rel="stylesheet" href="/overlay/lifeos-theme-overrides.css"> <script src="/overlay/lifeos-voice.js"></script> <script src="/overlay/lifeos-ambient-sense.js"></script>
</head>
<body>
<div class="app" id="app"> <!-- Sidebar --> <nav class="sidebar" id="sidebar"> <div class="sidebar-header"> <div class="logo"> <div class="logo-icon">◎</div> <span class="logo-text">Life<em>OS</em></span> </div> <button class="collapse-btn" id="collapse-btn" onclick="toggleSidebar()" title="Collapse sidebar"> ◀ </button> </div> <!-- User pill --> <div class="user-pill" id="user-pill" onclick="toggleUserDropdown()"> <div class="avatar" id="user-avatar">A</div> <div class="user-info"> <div class="user-name" id="sidebar-user-name">Adam</div> <div class="user-context" id="sidebar-current-context">Personal</div> <!-- NEW: Current context display --> <div class="user-handle" id="sidebar-user-handle">@adam</div> </div> <span class="chevron">▾</span> <div class="user-dropdown" id="user-dropdown"> <div class="dd-item" onclick="openSettings(event)">⚙ &nbsp;Settings</div> <!-- NEW: Context switching affordance --> <div class="dd-item" id="dd-switch-context" onclick="toggleContextPicker(event)"> 🏠 &nbsp;Switch Household/Context <span class="chevron-right" id="context-picker-chevron">›</span> </div> <div class="context-picker-dropdown" id="context-picker-dropdown"> <!-- Context items will be rendered here by JS --> </div> <div class="dd-item" onclick="signOut(event)">↩ &nbsp;Sign Out</div> </div> </div> <!-- Nav --> <div class="nav-scroll"> <div class="nav-group-label">Daily</div> <a class="nav-item active" id="nav-today" data-page="lifeos-today.html" data-label="Today" onclick="navigate(this)" href="#"> <span class="nav-icon">☀️</span> <span class="nav-label">Today</span> </a> <a class="nav-item" id="nav-dashboard" data-page="lifeos-dashboard.html" data-label="Dashboard" onclick="navigate(this)" href="#"> <span class="nav-icon">📊</span> <span class="nav-label">Dashboard</span> </a> <a class="nav-item" id="nav-chat" data-page="lifeos-chat.html" data-label="Lumin Chat" onclick="navigate(this)" href="#"> <span class="nav-icon">💬</span> <span class="nav-label">Chat</span> </a> <a class="nav-item" id="nav-mirror" data-page="lifeos-mirror.html" data-label="Mirror" onclick="navigate(this)" href="#"> <span class="nav-icon">🔮</span> <span class="nav-label">Mirror</span> </a> <a class="nav-item" id="nav-engine" data-page="lifeos-engine.html" data-label="Engine" onclick="navigate(this)" href="#"> <span class="nav-icon">⚡</span> <span class="nav-label">Engine</span> </a> <div class="nav-divider"></div> <div class="nav-group