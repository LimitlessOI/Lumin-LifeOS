# Phase 3 — Meta-25 (synthesized — **not** Phase-1 rerun)

**Exactly 25** new ids **`M01–M25`**. References Phase-1 tensions where helpful.

| ID | Meta-idea |
|----|-----------|
| M01 | **Prod-overlay CI smoke** — single workflow: **`npm run check:overlay`** + **`lifeos:supervise:static`** + **`builder:digest` tail** printed to **`§2.11b`** appendix — bundles **L01+L03** spirit. |
| M02 | **Tailwind exit plan as explicit queue row** — doc spike **`tailwind-build-spike`** before big bang (**anchors L02**). |
| M03 | **`aria-live` region for sync chip** — offline (**L20**) + builder digest errors surface without toast spam. |
| M04 | **Risk × slice_profile matrix** — CSV in amendment: which **`slice_profile`** allowed under **`risk: high`** (**L04+L13**). |
| M05 | **Staging lane JSON** — duplicate **`tasks[]`** stub file pointing at **`PUBLIC_BASE_URL_STAGING`** (**L06**). |
| M06 | **`/ready` exposes `queue_schema_revision`** next to **`codegen.policy_revision`** (**L07+L09**). |
| M07 | **Gaps replay as devDependency optional** — `npm run builder:gaps:replay -- --fixture` (**L08**). |
| M08 | **Incident ribbon** in daemon JSONL — human-readable cause when **`BUILDER_INCIDENT`** (**L05**). |
| M09 | **Critique-only pricing tag** — log **`estimated_tokens`** even when **`commit:false`** (**L15**). |
| M10 | **Emoji nav “long label” mode** — CSS toggled by **`prefers-reduced-motion`** off + **`data-nav-labels=full`** (**L18**). |
| M11 | **Dashboard `<section aria-labelledby>`** sweep — one PR, screenshot diff (**L17**). |
| M12 | **Density hydrated before paint** — inline script reads **`localStorage`** before body paints (**L19**). |
| M13 | **Victory Vault + MIT instrumentation shared envelope** — one **`telemetry`** helper (**L21+L22**). |
| M14 | **Semantic diff stored only on `committed:true`** — save $$ (**L12**). |
| M15 | **Orphan @ssot as weekly cron GitHub Action** not local-only (**L11**). |
| M16 | **Quiet-hours respects TZ env** — **`BUILDER_DAEMON_TZ`** (**L10**). |
| M17 | **Idea ledger read-only behind same auth as `/builder-health`** — reuse command key (**L23**). |
| M18 | **BranchPreview uses unified diff** — `diff -u` rendered in fenced block (**L24**). |
| M19 | **Retro template pulls last 4 `50_TRIAGE.md` rows** across sessions (**L25**). |
| M20 | **Rolling budget soft stop** — warn when hour projection exceeds **`BUILDER_QUEUE_TOKEN_BUDGET`** (**L14**). |
| M21 | **Voice PTT uses shared mic permission banner** — one-time explainer string (**L16**). |
| M22 | **Micro-splitter outputs manifest JSON** — list of **`files[]`** objects ready for **`POST /build`** (**L13**). |
| M23 | **DONE YAML includes `tests_required: []` explicit empty** — closes ambiguity (**L09**). |
| M24 | **Canary lane documents rollback** — one paragraph in **`BUILDER_OPERATOR_ENV`** (**L06**). |
| M25 | **“Honesty dashboard”** single Markdown emitted weekly: grade card + digest + triage delta (**M01+M19**). |
