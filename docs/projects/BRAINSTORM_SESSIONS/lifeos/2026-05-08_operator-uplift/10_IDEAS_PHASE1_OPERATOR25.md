# Phase 1 — Operator uplift burst (exactly 25)

**Convention:** prefixes per **`BRAINSTORM_SESSIONS_PROTOCOL.md`** Phase 1. **Audit:** Vault rows **20 (gaps admission) shipped opt-in**, **digest / risk-band** still NEXT — avoid duplicating verbatim; these push **beyond** vault list.

| ID | Idea |
|----|------|
| L01 | **[SYS]** Single **`npm run lifeos:e2e-golden`** Playwright lane — shell → iframe dashboard → mocked API smoke (aligns vault **#14**). |
| L02 | **[SYS]** Overlay **Tailwind removal** spike — CLI build + hashed CSS artefact wired in **`lifeos-app` + `lifeos-dashboard`** (closes prod console ERROR class). |
| L03 | **[SYS]** **`builder:digest`** script — rollup last N JSONL `{ ok, walls, gaps }` to Markdown for §2.11b (**vault #8**). |
| L04 | **[SYS]** Extend queue JSON **`risk`** enum + verifier read_table in amendment (**vault #1**). |
| L05 | **[SYS]** Incident env — **`BUILDER_INCIDENT=1`** forces **`supervise full` + BUILDER_QUEUE_MAX=1`. (**vault #13** spirit) |
| L06 | **[SYS]** Canary **`PUBLIC_BASE_URL_STAGING`** optional on **`lifeos-builder-continuous-queue`** (**vault #18**). |
| L07 | **[SYS]** **`queue.schema_revision`** int + coupling check rejects mismatch (**vault #24**). |
| L08 | **[SYS]** Replay fixture tests for **`/builder/gaps`** payloads (**vault #22**). |
| L09 | **[SYS]** Formal **DONE-definition YAML** surfaced in **`GET …/builder/ready`** codegen block (**vault #21**). |
| L10 | **[SYS]** Quiet-hours jitter on daemon sleep (**vault #19**). |
| L11 | **[SYS]** **`@ssot`↔manifest** orphaned-file weekly report in CI (**vault #23**). |
| L12 | **[SYS]** Semantic diff blobs stored adjacent to **`conductor_builder_audit`** rows (**vault #16**). |
| L13 | **[SYS]** Micro-task splitter CLI — chops fat HTML queue rows into chunked **`files[]`** targets (**vault #15** spirit). |
| L14 | **[SYS]** Rolling budget tokens / hour estimator before `/build` enqueue (**vault #5**) |
| L15 | **[SYS]** Critique-only second model POST flag (no commit) (**vault #11**) |
| L16 | **[PRODUCT:LifeOS]** Voice **PTT UX** tuning — waveform + idle state mirrored ChatGPT mobile prompt bar patterns. |
| L17 | **[PRODUCT:LifeOS]** **Mirrored headings** dashboard — visible region labels (“Today”, “Scores”, …) for SR + QA. |
| L18 | **[PRODUCT:LifeOS]** Emoji nav **paired text** tooltip + `aria-label` parity (fixes shell grade gap). |
| L19 | **[PRODUCT:LifeOS]** Density mode **`data-density`** hydration from `localStorage` per **`DASHBOARD_CUSTOMIZATION_STATE`**. |
| L20 | **[PRODUCT:LifeOS]** Offline queue UX — surfaced sync status chip when **`navigator.onLine`** flips. |
| L21 | **[PRODUCT:LifeOS]** Victory Vault **sidebar mount** wired per placement spec (**queue tail** ally). |
| L22 | **[PRODUCT:LifeOS]** **MIT widget** instrumentation — timings + error envelopes to **`telemetry`** spec rows. |
| L23 | **[NEW]** Operator **“idea ledger”** view — merges vault IDs + **`tasks[]`** + git tags in one **`/overlay`** diagnostics page (**read-only**) |
| L24 | **[NEW]** **`BranchPreview`** Markdown macro — renders diff between two **`00_CHARTER.md`** forks (ChatGPT branching metaphor in SSOT-native form) |
| L25 | **[NEW]** Quarterly **retro template** auto-filled from throughput meter receipts + **`50_TRIAGE` history**. |
