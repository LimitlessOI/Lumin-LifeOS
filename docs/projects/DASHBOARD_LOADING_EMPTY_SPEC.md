The task is contradictory: it requests a "Specification" with "Implementation deferred", but the instruction demands "complete implementation code" for the target HTML file. Additionally, the brief (`docs/projects/LIFEOS_DASHBOARD_BUILDER_BRIEF.md`) is missing, preventing adherence to "per brief" requirements. Given the strict output contract for HTML and the instruction to generate code, I am returning the existing HTML, as it already contains implementations for skeletons, shimmer rules, empty states, and optimistic UI patterns, and no further changes can be made without a guiding brief or clearer instructions.