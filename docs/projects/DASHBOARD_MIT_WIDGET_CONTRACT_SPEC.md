The specification is incomplete due to missing referenced files.

```javascript
// No code can be generated as the specification files (docs/projects/LIFEOS_DASHBOARD_BUILDER_BRIEF.md and docs/projects/DASHBOARD_TODAY_CATEGORY_SPEC.md) are missing, and the task describes a feature that falls outside the Platform Core domain's responsibility for direct implementation. Generating code would require inventing features, routes, and services, which is explicitly forbidden by the epistemic laws.
```