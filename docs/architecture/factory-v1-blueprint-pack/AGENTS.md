<!-- SYNOPSIS: Doctrine only / not runtime authority -->

# Doctrine only / not runtime authority

You are in the **factory v1 blueprint pack** — law and build specification, not executable runtime.

## What this layer is

- Constitutional factory vocabulary and phase specs
- BPB source material (`CODER_ZERO_DECISION_BUILD_SPEC_V1.md`, `FACTORY_A_TO_Z_BUILD_BLUEPRINT_V1.md`)
- Audits, salvage inventories, rebuild manifest

## Authority

- **Doctrine only** — defines what the system must become.
- **Docs alone earn zero runtime maturity** (`factory-staging/factory-core/canon/MATURITY_CLASSIFICATION.json`).
- Executable truth lives in `builderos-reboot/MISSIONS/` (machine) and `factory-staging/` (runtime).

## Before editing

1. Read `BLUEPRINT_PACK_INDEX_V1.md` and `FACTORY_REBUILD_MANIFEST_V1.md`
2. Obey SSOT read-before-write if changing constitutional meaning
3. When runtime changes, update manifest addenda and mission packs — not doctrine alone

## Rebuild entrypoint

```bash
npm run factory:ci
```

Start cold: `FACTORY_REBUILD_MANIFEST_V1.md` §1 read order.
