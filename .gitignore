node_modules
.DS_Store
.env
.env.local
.env.*.local

# Generated knowledge index (can be regenerated with scripts/process-knowledge.js)
knowledge/index/entries.jsonl
.cursor/*
!.cursor/BUGBOT.md
.next/
*.log
sandbox/
.temp_*.js
.worktrees/
pr-ref-2.patch
# Archive folder — old/backup files, never commit
archive/

# Backup and rollback artifacts (prevent repo pollution)
*.backup.*
*.pre-rollback.*
*_backup_*

# Self-programming artifacts
new_feature.js*
generated_*.js

# Legacy cleanup (can be removed after verification)
file_9.js
file_10.js

# FSAR reports (auto-generated, can be regenerated)
audit/reports/fsar_proposal_*.json
audit/reports/fsar_proposal_*.md

# Local quarantine + generated proof
.quarantine/
docs/THREAD_REALITY/outputs/**

# Machine-generated builder preflight audit (may contain deploy hostname; never secrets)
data/builder-preflight-log.jsonl
data/builder-overnight-log.jsonl
data/builder-daemon-log.jsonl
data/builder-daemon-state.json
data/builder-daemon.lock
data/token-efficiency-log.jsonl
data/cursor-pre-push-review*.json
# Local sequential cursor for scripts/lifeos-builder-overnight.mjs (OVERNIGHT_USE_CURSOR).
data/builder-overnight-cursor*.json
# Env certification probes (hostnames, pass/fail — never paste secret values into this file)
data/env-certification-log.jsonl
