```yaml
config:
  target: 'http://localhost:3000'
  phases:
    - duration: 60
      arrivalRate: 10
scenarios:
  - flow:
    - post:
        url: '/api/reports'
        json:
          title: 'Load Test Report'
          content: 'This is a load test of report generation.'
```