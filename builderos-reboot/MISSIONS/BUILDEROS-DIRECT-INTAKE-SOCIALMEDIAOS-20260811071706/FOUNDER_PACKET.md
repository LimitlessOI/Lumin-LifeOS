<!-- SYNOPSIS: Raw Founder Notes Intake — BUILDEROS-DIRECT-INTAKE-SOCIALMEDIAOS-20260811071706 -->

# Raw Founder Notes Intake — BUILDEROS-DIRECT-INTAKE-SOCIALMEDIAOS-20260811071706

Terminal direct intake entrypoint. Raw notes are intentionally unpolished.

## Raw Founder Notes
```text
Taloa screen-agent decision endpoint: real backend capability, not a session-only script. Tonight's native macOS Taloa overlay (native/macos-overlay/ContainerView.swift, ScreenControl.swift) already has two real, working, verified primitives: (1) full-screen capture via /usr/sbin/screencapture (captureFullScreen), (2) real system cursor move+click via CGEventPost (moveMouseAndClick), both self-granted via the Accessibility API, both live-verified with OS-level readback (cursor moved to exact requested coordinates, screen capture produced real 2992x1934 desktop content). Missing piece, named directly by the founder: connect these to the AI reasoning capability the system ALREADY has -- services/mtg-card-vision.js proves the exact call shape needed (OpenAI vision primary, Anthropic fallback, Gemini fallback -- SO-003 never-idle-on-tokens pattern), and services/voice-rail-attachments.js's describeVoiceRailImages already sends screenshots to OpenAI vision and gets back real descriptions, wired live in routes/lifeos-voice-rail-routes.js and services/voice-rail-v1.js. Both existing services only ever ask the AI to DESCRIBE an image (folded into a chat reply) -- never asked to return an actionable screen coordinate, and nothing currently takes that answer and executes a click. Build: a new backend endpoint (reusing the exact same vision-provider-fallback call shape already proven in mtg-card-vision.js -- same providers, same fallback chain, different prompt) that accepts a screenshot + a goal/instruction and returns a structured decision (e.g. {action: 'click'|'type'|'done'|'unclear', x, y, reasoning, confidence} in normalized 0-1 coordinates so it's resolution-independent across screen sizes). Founder's explicit, non-negotiable architecture requirement, stated directly twice this session: 'even if you're making capabilities for you, you need to make it for the system as well, that's what matters' -- this MUST be a real capability Taloa's own Swift code calls autonomously (a real HTTP call from ContainerView.swift/ScreenControl.swift to this new endpoint, parsing the response and calling the already-real moveMouseAndClick), not something only reachable by a human or an AI agent typing curl/python commands in a terminal session. The founder also explicitly named the one honest exception that must stay manual/external: no app can be given the capability to grant itself macOS system permissions (Accessibility/Screen Recording) -- that boundary is intentional OS security, not a gap to close. Ship through the governed factory per SO-001 (this is new services/routes code). Real acceptance test per CLAUDE.md's 'acceptance must prove reachability, not just existence' rule: grep-verify the new Swift call site actually exists in ContainerView.swift/ScreenControl.swift and actually invokes the new endpoint AND parses+acts on a real coordinate response -- a passing unit test on the backend endpoint alone is not sufficient, the Swift client wiring is part of done.
```
