function useWebGLFallback() {
  // Logic to detect device capabilities and switch to WebGL if VR is unavailable
  console.log('Using WebGL fallback for non-VR device');
}

module.exports = { useWebGLFallback };