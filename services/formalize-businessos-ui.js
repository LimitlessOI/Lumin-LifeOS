/**
 * SYNOPSIS: Existing code (if any) would be here
 */
// Existing code (if any) would be here

export function formalizeBusinessOSUI(uiName) {
  return uiName.replace(/BusinessOS/g, 'LimitlessOS');
}
