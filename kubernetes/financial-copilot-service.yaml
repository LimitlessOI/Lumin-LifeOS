# SYNOPSIS: yaml — kubernetes/financial-copilot-service.yaml.
```yaml
apiVersion: v1
kind: Service
metadata:
  name: financial-copilot
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: financial-copilot
```