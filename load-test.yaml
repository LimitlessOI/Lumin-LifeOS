config:
  target: 'http://localhost:3000'
  phases:
    - duration: 60
      arrivalRate: 5
scenarios:
  - flow:
    - post:
        url: '/users/signup'
        json:
          username: 'user{{ uuid }}'
          password: 'password'
          email: 'user{{ uuid }}@example.com'