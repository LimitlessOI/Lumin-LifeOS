```json
{
  "name": "user-management-system",
  "version": "1.0.0",
  "description": "A system for managing users with migration and testing.",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "migrate": "node scripts/run-migration.js"
  },
  "dependencies": {
    "express": "^4.17.1",
    "flyway-core": "^x.x.x",
    "pg": "^x.x.x",
    "node-fetch": "^x.x.x"
  },
  "devDependencies": {
    "jest": "^x.x.x",
    "supertest": "^x.x.x"
  }
}
```